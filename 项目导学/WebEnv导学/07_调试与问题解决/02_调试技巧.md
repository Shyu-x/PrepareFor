# 调试技巧

本章节将详细介绍 WebEnv 项目开发过程中常用的调试技巧和工具。

## 1. 浏览器开发者工具使用

### 1.1 Console 技巧

**基础 Console 方法：**

```tsx
// 基本输出
console.log('普通信息');
console.info('信息');
console.warn('警告');
console.error('错误');

// 格式化输出
console.log('用户: %s, 年龄: %d', 'John', 25);
console.log({ user: { name: 'John', age: 25 } });

// 样式输出
console.log('%c这是红色大字', 'color: red; font-size: 20px;');
console.log('%c蓝色 %c绿色', 'color: blue;', 'color: green;');

// 表格输出
console.table([
  { name: 'John', age: 25 },
  { name: 'Jane', age: 30 },
]);

// 分组输出
console.group('用户信息');
console.log('Name: John');
console.log('Age: 25');
console.groupEnd();

// 计时
console.time('fetchData');
fetch('/api/data').then(() => {
  console.timeEnd('fetchData');
});

// 计数
function testFunction() {
  console.count('testFunction');
}
```

**条件断点技巧：**

在 Chrome DevTools 中：
1. 打开 Sources 面板
2. 在代码行号上右键
3. 选择 "Add conditional breakpoint"
4. 输入条件表达式，如：`count > 10`

### 1.2 Network 面板技巧

**过滤请求：**

```
// 按类型过滤
- status-code:200
- method:POST
- domain:localhost

// 按大小过滤
-larger-than:1000

// 组合过滤
method:GET status-code:200 domain:api.example.com
```

**重放请求：**

1. 右键点击请求
2. 选择 "Replay XHR" / "Fetch again"

**复制 cURL：**

1. 右键点击请求
2. 选择 "Copy" → "Copy as cURL"

### 1.3 Elements 面板技巧

**快速查找元素：**

```bash
# 使用快捷键 Ctrl+Shift+P
# 输入 "Show file"
```

**查看事件监听器：**

1. 选中元素
2. 查看右侧 "Event Listeners" 面板
3. 勾选 "Ancestors" 查看父元素事件

**CSS 调试技巧：**

```css
/* 临时调试样式 */
.element {
  outline: 2px solid red !important;
  background: rgba(255, 0, 0, 0.1) !important;
}
```

## 2. React DevTools

### 2.1 安装和使用

**安装：**
- Chrome: React Developer Tools 扩展
- Firefox: React Developer Tools 扩展

**启动：**
- Components 面板：查看组件树和 props
- Profiler 面板：记录和分析性能

### 2.2 Components 面板使用

**查看组件状态：**

```tsx
function UserProfile() {
  // 在 DevTools 中可以看到这些状态
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  // 在 DevTools 中可以修改这些值进行测试
  const [theme, setTheme] = useState('light');

  return <div>{user?.name}</div>;
}
```

**使用 useDebugValue：**

```tsx
function useOnlineStatus() {
  const [isOnline, setIsOnline] = useState(navigator.onLine);

  // 在 DevTools 中显示自定义标签
  useDebugValue(isOnline ? 'Online' : 'Offline');

  useEffect(() => {
    const handleOnline = () => setIsOnline(true);
    const handleOffline = () => setIsOnline(false);

    window.addEventListener('online', handleOnline);
    window.addEventListener('offline', handleOffline);

    return () => {
      window.removeEventListener('online', handleOnline);
      window.removeEventListener('offline', handleOffline);
    };
  }, []);

  return isOnline;
}
```

### 2.3 Profiler 面板使用

**记录性能：**

1. 打开 Profiler 面板
2. 点击 "Record" 按钮
3. 执行操作
4. 点击 "Stop" 按钮

**分析结果：**

- **Flamegraph**：查看渲染时间分布
- **Ranked**：按渲染时间排序的组件列表
- **Timings**：详细的时间信息

**解读性能问题：**

```
// 常见性能问题的标志：
// 1. 组件渲染时间过长（黄色/红色）
// 2. 组件渲染次数过多
// 3. 某些组件频繁重新渲染
```

## 3. Redux/Zustand DevTools

### 3.1 Zustand DevTools 配置

**基础配置：**

```tsx
import { create } from 'zustand';
import { devtools } from 'zustand/middleware';

const useStore = create(
  devtools(
    (set) => ({
      count: 0,
      increment: () => set((state) => ({ count: state.count + 1 })),
      decrement: () => set((state) => ({ count: state.count - 1 })),
      reset: () => set({ count: 0 }),
    }),
    { name: 'my-store' } // 在 DevTools 中的名称
  )
);
```

**使用 React DevTools 集成：**

```tsx
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';

const useStore = create(
  persist(
    (set) => ({
      theme: 'light',
      setTheme: (theme: string) => set({ theme }),
    }),
    {
      name: 'theme-storage',
      storage: createJSONStorage(() => localStorage),
    }
  )
);
```

### 3.2 调试技巧

**查看状态历史：**

在 Zustand DevTools 中可以：
- 查看所有状态变化
- 跳转到任意历史状态
- 撤销/重做操作

**调试状态问题：**

```tsx
// 添加日志中间件
import { create } from 'zustand';
import { subscribeWithSelector } from 'zustand/middleware';

const useStore = create(
  subscribeWithSelector(
    (set) => ({
      items: [],
      addItem: (item: string) =>
        set((state) => {
          console.log('Adding item:', item, 'Current state:', state);
          return { items: [...state.items, item] };
        }),
      removeItem: (index: number) =>
        set((state) => {
          console.log('Removing item at:', index, 'Current state:', state);
          return {
            items: state.items.filter((_, i) => i !== index),
          };
        }),
    })
  )
);
```

## 4. Monaco Editor 调试

### 4.1 编辑器实例调试

**获取编辑器实例：**

```tsx
import Editor from '@monaco-editor/react';
import * as monaco from 'monaco-editor';

function CodeEditor() {
  const editorRef = useRef<monaco.editor.IStandaloneCodeEditor | null>(null);

  const handleEditorDidMount = (
    editor: monaco.editor.IStandaloneCodeEditor,
    monaco: typeof Monaco
  ) => {
    editorRef.current = editor;

    // 获取编辑器信息
    console.log('Editor mounted:', {
      value: editor.getValue(),
      language: editor.getModel()?.getLanguageId(),
      lineCount: editor.getModel()?.getLineCount(),
    });

    // 监听内容变化
    editor.onDidChangeModelContent(() => {
      console.log('Content changed:', editor.getValue());
    });
  };

  return (
    <Editor
      height="500px"
      defaultLanguage="javascript"
      defaultValue="// Write your code here"
      onMount={handleEditorDidMount}
    />
  );
}
```

### 4.2 自定义调试功能

**添加自定义命令：**

```tsx
function useEditorCommands(editor: monaco.editor.IStandaloneCodeEditor) {
  useEffect(() => {
    // 注册自定义命令
    editor.addCommand(monaco.KeyMod.CtrlCmd | monaco.KeyCode.KeyS, () => {
      console.log('Save command triggered');
      // 保存逻辑
    });

    // 注册自定义操作
    editor.addAction({
      id: 'custom-format',
      label: 'Custom Format',
      keybindings: [monaco.KeyMod.CtrlCmd | monaco.KeyCode.KeyF],
      run: (ed) => {
        console.log('Format action triggered');
      },
    });
  }, [editor]);
}
```

### 4.3 语言服务调试

**配置诊断：**

```tsx
<Editor
  defaultLanguage="typescript"
  defaultValue="const x: string = 1; // 错误类型"
  onMount={(editor, monaco) => {
    // 监听标记变化（错误、警告等）
    editor.onDidChangeMarkers((uris) => {
      const model = editor.getModel();
      if (model) {
        const markers = monaco.editor.getModelMarkers({ resource: model.uri });
        console.log('Markers:', markers);
      }
    });
  }}
/>
```

## 5. 网络请求调试

### 5.1 fetch 请求调试

**基础调试：**

```tsx
async function fetchUserData(userId: string) {
  try {
    console.log('Fetching user:', userId);

    const response = await fetch(`/api/users/${userId}`);

    console.log('Response status:', response.status);
    console.log('Response headers:', Object.fromEntries(response.headers));

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const data = await response.json();
    console.log('Response data:', data);

    return data;
  } catch (error) {
    console.error('Fetch error:', error);
    throw error;
  }
}
```

**使用 AbortController 取消请求：**

```tsx
function useFetchWithCancel(url: string) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const controller = new AbortController();
    const signal = controller.signal;

    setLoading(true);
    setError(null);

    fetch(url, { signal })
      .then((response) => response.json())
      .then((data) => {
        if (!signal.aborted) {
          setData(data);
          setLoading(false);
        }
      })
      .catch((err) => {
        if (err.name !== 'AbortError') {
          setError(err);
          setLoading(false);
        }
      });

    return () => controller.abort();
  }, [url]);

  return { data, loading, error };
}
```

### 5.2 SWR 调试

**调试配置：**

```tsx
import useSWR from 'swr';

const fetcher = (url: string) => {
  console.log('Fetching:', url);
  return fetch(url).then((res) => {
    if (!res.ok) {
      throw new Error(`HTTP error! status: ${res.status}`);
    }
    return res.json();
  });
};

function UserProfile({ userId }: { userId: string }) {
  const { data, error, isLoading, mutate } = useSWR(
    `/api/users/${userId}`,
    fetcher,
    {
      onSuccess: (data) => {
        console.log('SWR Success:', data);
      },
      onError: (err) => {
        console.error('SWR Error:', err);
      },
      revalidateOnFocus: false,
      revalidateOnReconnect: false,
    }
  );

  if (isLoading) return <div>Loading...</div>;
  if (error) return <div>Error: {error.message}</div>;

  return (
    <div>
      <h1>{data.name}</h1>
      <button onClick={() => mutate()}>Revalidate</button>
    </div>
  );
}
```

### 5.3 Socket.io 调试

**连接调试：**

```tsx
import { useEffect, useRef } from 'react';
import { io, Socket } from 'socket.io-client';

function useSocketDebug(url: string) {
  const socketRef = useRef<Socket | null>(null);

  useEffect(() => {
    const socket = io(url, {
      transports: ['websocket'],
      reconnection: true,
      reconnectionAttempts: 5,
      reconnectionDelay: 1000,
    });

    socketRef.current = socket;

    // 监听所有事件
    socket.onAny((eventName, ...args) => {
      console.log(`[Socket] ${eventName}:`, args);
    });

    // 连接事件
    socket.on('connect', () => {
      console.log('[Socket] Connected:', socket.id);
    });

    socket.on('disconnect', (reason) => {
      console.log('[Socket] Disconnected:', reason);
    });

    socket.on('connect_error', (error) => {
      console.error('[Socket] Connection error:', error.message);
    });

    return () => {
      socket.disconnect();
    };
  }, [url]);

  return socketRef;
}
```

## 6. 控制台技巧

### 6.1 高级 Console 方法

```tsx
// 断言（只在条件为 false 时输出）
console.assert(condition, 'This should not happen');

// 计数（带标签）
console.count('click');
console.countReset('click');

// 调试（仅在开发环境显示）
console.debug('Debug info');

// 跟踪（打印堆栈跟踪）
function inner() {
  console.trace('Stack trace');
}
function outer() {
  inner();
}
outer();

// 时间
console.time('loop');
for (let i = 0; i < 10000; i++) {}
console.timeEnd('loop');

// 表格
console.table([
  { name: 'John', role: 'Admin' },
  { name: 'Jane', role: 'User' },
]);

// 分组
console.group('Request');
console.log('URL: /api/users');
console.log('Method: GET');
console.groupEnd();
```

### 6.2 条件断点调试

**在代码中设置条件断点：**

```tsx
function processItems(items: any[]) {
  // 使用 debugger 语句
  items.forEach((item, index) => {
    if (index === 50) {
      debugger; // 会在第50个元素时暂停
    }
    processItem(item);
  });
}

// 使用条件断点函数
function debugWhen(condition: boolean) {
  if (condition) {
    debugger;
  }
}
```

### 6.3 性能调试

```tsx
// 测量函数执行时间
function measurePerformance<T extends (...args: any[]) => any>(
  fn: T,
  label: string
): T {
  return ((...args: any[]) => {
    const start = performance.now();
    const result = fn(...args);
    const end = performance.now();
    console.log(`[Performance] ${label}: ${(end - start).toFixed(2)}ms`);
    return result;
  }) as T;
}

// 使用
const slowFunction = measurePerformance(
  (n: number) => {
    // 耗时操作
    return Array(n).fill(0).map((_, i) => i * 2);
  },
  'slowFunction'
);
```

### 6.4 React 组件调试 Hook

```tsx
// 自定义调试 Hook
function useRenderCount() {
  const count = useRef(0);
  count.current++;

  useEffect(() => {
    console.log(`Component rendered ${count.current} times`);
  });

  return count.current;
}

// 使用
function DebugComponent() {
  const renderCount = useRenderCount();

  useEffect(() => {
    console.log('Component mounted');

    return () => {
      console.log('Component unmounted');
    };
  }, []);

  return <div>Render count: {renderCount}</div>;
}
```

## 7. 实战调试练习

### 练习 1：调试状态更新问题

**问题描述：** 组件无限重新渲染

```tsx
// 问题代码
function Counter() {
  const [count, setCount] = useState(0);

  // 这个 useEffect 会导致无限循环
  useEffect(() => {
    setCount(count + 1);
  }, [count]);

  return <div>{count}</div>;
}

// 修复方案
function CounterFixed() {
  const [count, setCount] = useState(0);

  // 正确的做法：只在挂载时执行一次
  useEffect(() => {
    // 初始化逻辑
  }, []); // 空依赖数组

  const increment = () => setCount(c => c + 1);

  return (
    <div>
      <span>{count}</span>
      <button onClick={increment}>Increment</button>
    </div>
  );
}
```

### 练习 2：调试 API 请求失败

```tsx
// 问题：API 请求失败但没有错误处理
function UserList() {
  const [users, setUsers] = useState([]);

  useEffect(() => {
    fetch('/api/users')
      .then(res => res.json())
      .then(data => setUsers(data));
    // 缺少错误处理
  }, []);

  return (
    <ul>
      {users.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}

// 修复方案
function UserListFixed() {
  const [users, setUsers] = useState([]);
  const [error, setError] = useState<string | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    let cancelled = false;

    async function loadUsers() {
      try {
        const res = await fetch('/api/users');
        if (!res.ok) {
          throw new Error(`HTTP ${res.status}`);
        }
        const data = await res.json();
        if (!cancelled) {
          setUsers(data);
        }
      } catch (err) {
        if (!cancelled) {
          setError(err instanceof Error ? err.message : 'Failed to load users');
        }
      } finally {
        if (!cancelled) {
          setLoading(false);
        }
      }
    }

    loadUsers();

    return () => {
      cancelled = true;
    };
  }, []);

  if (loading) return <div>Loading...</div>;
  if (error) return <div>Error: {error}</div>;

  return (
    <ul>
      {users.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}
```

### 练习 3：调试 Monaco Editor 不加载

```tsx
// 问题：Monaco Editor 在某些情况下不加载
function CodeEditor() {
  const [code, setCode] = useState('');

  return (
    <Editor
      height="500px"
      // 缺少 language 和 value
      onChange={(value) => setCode(value || '')}
    />
  );
}

// 修复方案
function CodeEditorFixed() {
  const [code, setCode] = useState('// Start coding here');

  return (
    <Editor
      height="500px"
      defaultLanguage="javascript"
      defaultValue={code}
      theme="vs-dark"
      onChange={(value) => setCode(value || '')}
      loading={<div>Loading editor...</div>}
      options={{
        minimap: { enabled: true },
        fontSize: 14,
        lineNumbers: 'on',
        scrollBeyondLastLine: false,
        automaticLayout: true,
      }}
    />
  );
}
```

## 8. 调试工具总结

| 工具 | 用途 | 快捷键 |
|-----|-----|-------|
| Chrome DevTools | 通用调试 | F12 |
| React DevTools | React 组件调试 | - |
| Zustand DevTools | 状态管理调试 | - |
| Monaco Editor | 代码编辑调试 | - |
| Network 面板 | 网络请求调试 | - |
| Console | 控制台输出 | - |

## 最佳实践

1. **使用条件断点** - 减少不必要的暂停
2. **合理使用 console** - 区分不同级别的日志
3. **使用 DevTools** - 结合多种工具进行调试
4. **性能分析** - 使用 React Profiler 查找瓶颈
5. **错误边界** - 捕获并记录组件错误
