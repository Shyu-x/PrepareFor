# 状态管理完全指南

## 目录

1. [状态管理基础](#1-状态管理基础)
2. [Zustand深度解析](#2-zustand深度解析)
3. [Redux Toolkit实战](#3-redux-toolkit实战)
4. [Context API与useReducer](#4-context-api与usereducer)
5. [Recoil与Jotai](#5-recoil与jotai)
6. [状态管理最佳实践](#6-状态管理最佳实践)

---

## 1. 状态管理基础

### 1.1 状态类型

```
状态管理层次结构

┌─────────────────────────────────────┐
│   全局状态（Global State）          │
│   - 用户信息、主题、通知             │
│   - 跨多个组件共享                   │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│   模块状态（Module State）          │
│   - 购物车、表单、列表               │
│   - 特定功能模块                     │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│   组件状态（Component State）       │
│   - 按钮点击、输入框值、模态框       │
│   - 单个组件内部                     │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│   衍生状态（Derived State）         │
│   - 过滤后的列表、格式化的日期       │
│   - 从其他状态计算得出               │
└─────────────────────────────────────┘
```

### 1.2 状态管理方案对比

| 方案 | 复杂度 | 学习曲线 | 性能 | 适用场景 |
|------|--------|----------|------|----------|
| **useState** | ⭐ 低 | 🟢 简单 | ✅ 高 | 简单组件状态 |
| **Context API** | ⭐⭐ 中 | 🟡 中等 | ⚠️ 低 | 跨组件共享 |
| **Zustand** | ⭐⭐ 中 | 🟢 简单 | ✅ 高 | 中小型项目 |
| **Redux Toolkit** | ⭐⭐⭐ 高 | 🔴 陡峭 | ✅ 高 | 大型复杂项目 |
| **Recoil** | ⭐⭐⭐ 高 | 🟡 中等 | ✅ 高 | 原子化状态 |
| **Jotai** | ⭐⭐ 中 | 🟢 简单 | ✅ 高 | 原子化状态 |

### 1.3 选择指南

```javascript
// 状态选择决策树
function chooseStateManagement(scenario) {
  if (scenario.componentOnly) {
    return 'useState';  // 组件内部状态
  }

  if (scenario.simpleSharing && !scenario.frequentUpdates) {
    return 'Context API';  // 简单跨组件共享
  }

  if (scenario.complexLogic || scenario.timeTravel) {
    return 'Redux Toolkit';  // 复杂业务逻辑
  }

  if (scenario.needPersistence || scenario.middlewareSupport) {
    return 'Zustand';  // 需要持久化和中间件
  }

  if (scenario.atomicState || scenario.atomFamily) {
    return 'Recoil';  // 原子化状态管理
  }

  if (scenario.smallProject && scenario.boilerplateFree) {
    return 'Zustand';  // 无样板代码
  }

  return 'useState';  // 默认方案
}
```

---

## 2. Zustand深度解析

### 2.1 基础用法

```javascript
// store/useUserStore.ts
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';

// 定义状态类型
interface User {
  id: number;
  name: string;
  email: string;
  avatar?: string;
}

interface UserState {
  user: User | null;
  isLoading: boolean;
  error: string | null;

  // Actions
  setUser: (user: User) => void;
  clearUser: () => void;
  fetchUser: (id: number) => Promise<void>;
  updateUser: (updates: Partial<User>) => void;
}

// 创建Store
export const useUserStore = create<UserState>()(
  persist(
    (set, get) => ({
      // 初始状态
      user: null,
      isLoading: false,
      error: null,

      // 设置用户
      setUser: (user) => set({ user, error: null }),

      // 清除用户
      clearUser: () => set({ user: null, error: null }),

      // 异步获取用户
      fetchUser: async (id) => {
        set({ isLoading: true, error: null });
        try {
          const response = await fetch(`/api/users/${id}`);
          const user = await response.json();
          set({ user, isLoading: false });
        } catch (error) {
          set({
            error: '获取用户失败',
            isLoading: false,
          });
        }
      },

      // 更新用户信息
      updateUser: (updates) =>
        set((state) => ({
          user: state.user ? { ...state.user, ...updates } : null,
        })),
    }),
    {
      // 持久化配置
      name: 'user-storage',
      storage: createJSONStorage(() => localStorage),
      // 只持久化user字段
      partialize: (state) => ({ user: state.user }),
    }
  )
);

// 在组件中使用
import { useUserStore } from '@/store/useUserStore';

function UserProfile() {
  const { user, isLoading, error, fetchUser, updateUser } = useUserStore();

  useEffect(() => {
    fetchUser(1);
  }, [fetchUser]);

  if (isLoading) return <div>加载中...</div>;
  if (error) return <div>{error}</div>;

  return (
    <div>
      <h1>{user?.name}</h1>
      <p>{user?.email}</p>
      <button onClick={() => updateUser({ name: '新名字' })}>
        更新名字
      </button>
    </div>
  );
}
```

### 2.2 Zustand高级特性

```javascript
// 1. 选择器优化（避免不必要的重渲染）
function UserAvatar() {
  // ❌ 每次store更新都会重渲染
  const state = useUserStore();

  // ✅ 只订阅user字段
  const user = useUserStore((state) => state.user);

  // ✅ 使用shallow进行浅比较
  const { user, isLoading } = useUserStore(
    (state) => ({ user: state.user, isLoading: state.isLoading }),
    shallow
  );

  // ✅ 自定义选择器
  const userName = useUserStore((state) => state.user?.name);

  return <img src={user?.avatar} alt={user?.name} />;
}

// 2. 异步Action模式
interface AsyncState {
  data: any[];
  loading: boolean;
  error: string | null;
}

export const useAsyncStore = create<AsyncState>((set) => ({
  data: [],
  loading: false,
  error: null,

  // 异步Action
  fetchData: async (url) => {
    set({ loading: true, error: null });
    try {
      const response = await fetch(url);
      const data = await response.json();
      set({ data, loading: false });
    } catch (error) {
      set({
        error: error.message,
        loading: false,
      });
    }
  },
}));

// 3. 组合多个Store
// useCombinedStore.ts
export const useCombinedStore = create((set, get) => ({
  ...useUserStore.getState(),
  ...useCartStore.getState(),

  // 跨Store操作
  addUserToCart: (userId, productId) => {
    const user = useUserStore.getState().user;
    const cart = useCartStore.getState();
    cart.addItem({ userId, productId });
  },
}));

// 4. 中间件系统
import { devtools, persist, subscribeWithSelector } from 'zustand/middleware';

export const useStore = create()(
  devtools(
    persist(
      subscribeWithSelector((set, get) => ({
        // ...store定义
      })),
      {
        name: 'my-storage',
        storage: createJSONStorage(() => localStorage),
      }
    ),
    {
      name: 'my-store',
      enabled: process.env.NODE_ENV === 'development',
    }
  )
);

// 5. 订阅Store变化
// 在组件外部订阅
const unsubscribe = useUserStore.subscribe(
  (state) => state.user,
  (user) => {
    console.log('用户已更新:', user);
    // 发送分析事件
    analytics.track('user_updated', { user });
  },
  {
    // 配置选项
    equalityFn: shallow,  // 浅比较
    fireImmediately: true,  // 立即执行
  }
);

// 6. Store重置
const useResetStore = create((set) => ({
  // ...store定义
  reset: () => set(initialState),
}));

// 7. TypeScript类型推导
interface BearState {
  bears: number;
  increase: (by: number) => void;
}

const useBearStore = create<BearState>()((set) => ({
  bears: 0,
  increase: (by) => set((state) => ({ bears: state.bears + by })),
}));
```

### 2.3 Zustand中间件

```javascript
// 1. devtools中间件
import { devtools } from 'zustand/middleware';

const useStore = create(
  devtools(
    (set) => ({
      count: 0,
      increment: () => set((state) => ({ count: state.count + 1 }), false, 'increment'),
    }),
    {
      name: 'my-store',  // Store名称
      enabled: process.env.NODE_ENV === 'development',
    }
  )
);

// 2. persist中间件
import { persist, createJSONStorage } from 'zustand/middleware';

const useStore = create(
  persist(
    (set) => ({
      user: null,
      setUser: (user) => set({ user }),
    }),
    {
      name: 'user-storage',
      storage: createJSONStorage(() => localStorage),
      // 只持久化部分状态
      partialize: (state) => ({ user: state.user }),
      // 版本控制
      version: 1,
      // 迁移函数
      migrate: (persistedState, version) => {
        if (version === 0) {
          // 从v0迁移到v1
          return {
            ...persistedState,
            user: {
              ...persistedState.user,
              newName: 'default',
            },
          };
        }
        return persistedState;
      },
    }
  )
);

// 3. immer中间件（不可变更新）
import { immer } from 'zustand/middleware/immer';

const useStore = create(
  immer((set) => ({
    users: [],
    addUser: (user) =>
      set((state) => {
        state.users.push(user);  // 直接修改
      }),
    updateUser: (id, updates) =>
      set((state) => {
        const user = state.users.find((u) => u.id === id);
        if (user) {
          Object.assign(user, updates);  // 合并更新
        }
      }),
  }))
);

// 4. combine中间件（组合多个store）
import { combine } from 'zustand/middleware';

const useUserStore = create(
  combine(
    { user: null },
    (set) => ({
      setUser: (user) => set({ user }),
    })
  )
);

const useCartStore = create(
  combine(
    { items: [] },
    (set) => ({
      addItem: (item) =>
        set((state) => ({ items: [...state.items, item] })),
    })
  )
);

// 5. 订阅选择器中间件
import { subscribeWithSelector } from 'zustand/middleware';

const useStore = create(
  subscribeWithSelector((set) => ({
    items: [],
    addItem: (item) =>
      set((state) => ({ items: [...state.items, item] })),
  }))
);

// 订阅特定字段变化
useStore.subscribe(
  (state) => state.items,
  (items) => {
    console.log('购物车已更新:', items);
  }
);
```

### 2.4 Zustand最佳实践

```javascript
// 1. 状态设计原则
// ✅ 好的设计：扁平化状态
interface GoodState {
  user: { id: number; name: string; email: string } | null;
  products: Array<{ id: number; name: string; price: number }>;
  cart: Array<{ productId: number; quantity: number }>;
}

// ❌ 不好的设计：嵌套过深
interface BadState {
  data: {
    user: {
      profile: {
        personal: {
          name: string;
          email: string;
        };
      };
    };
  };
}

// 2. Action设计
// ✅ 好的设计：单一职责
const setUser = (user: User) => set({ user });
const updateUser = (updates: Partial<User>) =>
  set((state) => ({
    user: state.user ? { ...state.user, ...updates } : null,
  }));

// ❌ 不好的设计：一个Action做多件事
const setUserAndDoEverything = async (user: User) => {
  // 设置用户
  // 发送API请求
  // 更新购物车
  // 记录日志
  // ...太多职责
};

// 3. Store拆分
// store/userStore.ts
export const useUserStore = create<UserState>((set) => ({ /* ... */ }));

// store/cartStore.ts
export const useCartStore = create<CartState>((set) => ({ /* ... */ }));

// store/index.ts
export { useUserStore } from './userStore';
export { useCartStore } from './cartStore';

// 4. 类型安全
interface UserState {
  user: User | null;
  setUser: (user: User) => void;
}

export const useUserStore = create<UserState>((set) => ({ /* ... */ }));

// 使用类型推导
const setUser = (user: User) => {
  const { setUser: setUserAction } = useUserStore.getState();
  setUserAction(user);  // TypeScript会检查类型
};
```

---

## 3. Redux Toolkit实战

### 3.1 Redux Toolkit基础

```javascript
// store/userSlice.ts
import { createSlice, createAsyncThunk, PayloadAction } from '@reduxjs/toolkit';

// 定义类型
interface User {
  id: number;
  name: string;
  email: string;
}

interface UserState {
  user: User | null;
  loading: boolean;
  error: string | null;
}

// 异步Thunk
export const fetchUser = createAsyncThunk(
  'user/fetchUser',
  async (userId: number, { rejectWithValue }) => {
    try {
      const response = await fetch(`/api/users/${userId}`);
      if (!response.ok) {
        throw new Error('获取用户失败');
      }
      return await response.json();
    } catch (error) {
      return rejectWithValue(error.message);
    }
  }
);

// 创建Slice
const userSlice = createSlice({
  name: 'user',
  initialState: {
    user: null,
    loading: false,
    error: null,
  } as UserState,
  reducers: {
    setUser: (state, action: PayloadAction<User>) => {
      state.user = action.payload;
      state.error = null;
    },
    clearUser: (state) => {
      state.user = null;
      state.error = null;
    },
    updateUser: (state, action: PayloadAction<Partial<User>>) => {
      if (state.user) {
        Object.assign(state.user, action.payload);
      }
    },
  },
  extraReducers: (builder) => {
    builder
      .addCase(fetchUser.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(fetchUser.fulfilled, (state, action) => {
        state.loading = false;
        state.user = action.payload;
      })
      .addCase(fetchUser.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload as string;
      });
  },
});

export const { setUser, clearUser, updateUser } = userSlice.actions;
export default userSlice.reducer;

// store/index.ts
import { configureStore } from '@reduxjs/toolkit';
import userReducer from './userSlice';
import cartReducer from './cartSlice';

export const store = configureStore({
  reducer: {
    user: userReducer,
    cart: cartReducer,
  },
  // 中间件
  middleware: (getDefaultMiddleware) =>
    getDefaultMiddleware({
      serializableCheck: {
        ignoredActions: ['user/fetchUser/fulfilled'],
      },
    }),
  // 开发工具
  devTools: process.env.NODE_ENV !== 'production',
});

// 类型推导
export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;

// Hooks
import { useDispatch, useSelector } from 'react-redux';
import type { TypedUseSelectorHook } from 'react-redux';

export const useAppDispatch: () => AppDispatch = useDispatch;
export const useAppSelector: TypedUseSelectorHook<RootState> = useSelector;

// 在组件中使用
import { useAppDispatch, useAppSelector } from '@/store';
import { fetchUser, setUser } from '@/store/userSlice';

function UserProfile() {
  const dispatch = useAppDispatch();
  const { user, loading, error } = useAppSelector((state) => state.user);

  useEffect(() => {
    dispatch(fetchUser(1));
  }, [dispatch]);

  const handleUpdateName = (name: string) => {
    dispatch(setUser({ ...user, name }));
  };

  if (loading) return <div>加载中...</div>;
  if (error) return <div>{error}</div>;

  return (
    <div>
      <h1>{user?.name}</h1>
      <p>{user?.email}</p>
    </div>
  );
}
```

### 3.2 Redux Toolkit高级用法

```javascript
// 1. 自定义Thunk
export const updateUserWithValidation = createAsyncThunk(
  'user/updateUser',
  async (
    { userId, updates }: { userId: number; updates: Partial<User> },
    { rejectWithValue, getState }
  ) => {
    const state = getState() as RootState;

    // 验证
    if (!state.user.user) {
      return rejectWithValue('用户未登录');
    }

    try {
      const response = await fetch(`/api/users/${userId}`, {
        method: 'PATCH',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(updates),
      });

      if (!response.ok) {
        throw new Error('更新失败');
      }

      return await response.json();
    } catch (error) {
      return rejectWithValue(error.message);
    }
  }
);

// 2. Thunk组合
export const fetchUserAndCart = createAsyncThunk(
  'app/fetchUserAndCart',
  async (userId: number, { dispatch }) => {
    // 并发请求
    await Promise.all([
      dispatch(fetchUser(userId)).unwrap(),
      dispatch(fetchCart(userId)).unwrap(),
    ]);
  }
);

// 3. 创建AsyncThunk组合
export const createUserAndInitializeCart = createAsyncThunk(
  'app/createUserAndInitializeCart',
  async (userData: CreateUserDto, { dispatch }) => {
    // 创建用户
    const user = await dispatch(fetchCreateUser(userData)).unwrap();

    // 初始化购物车
    await dispatch(fetchCart(user.id)).unwrap();

    return user;
  }
);

// 4. Normalizr（数据规范化）
import { normalize, schema } from 'normalizr';

const userSchema = new schema.Entity('users');
const productSchema = new schema.Entity('products');

const normalizedData = normalize(
  {
    id: 1,
    name: '张三',
    products: [
      { id: 101, name: '产品A' },
      { id: 102, name: '产品B' },
    ],
  },
  {
    products: [productSchema],
  }
);

// 5. 选择器
// store/selectors.ts
import { createSelector } from '@reduxjs/toolkit';

export const selectUser = (state: RootState) => state.user.user;
export const selectUserLoading = (state: RootState) => state.user.loading;

// 记忆化选择器
export const selectUserDisplayName = createSelector(
  [selectUser],
  (user) => `${user?.name} (${user?.email})`
);

// 复合选择器
export const selectCartTotal = createSelector(
  [(state: RootState) => state.cart.items],
  (items) => items.reduce((total, item) => total + item.price * item.quantity, 0)
);

// 6. 中间件
import { createListenerMiddleware } from '@reduxjs/toolkit';

const listenerMiddleware = createListenerMiddleware();

listenerMiddleware.startListening({
  actionCreator: setUser,
  effect: async (action, listenerApi) => {
    console.log('用户已设置:', action.payload);

    // 自动加载用户购物车
    await listenerApi.dispatch(fetchCart(action.payload.id));
  },
});

// 添加到store
export const store = configureStore({
  reducer: {
    user: userReducer,
    cart: cartReducer,
  },
  middleware: (getDefaultMiddleware) =>
    getDefaultMiddleware().prepend(listenerMiddleware.middleware),
});
```

### 3.3 Redux Toolkit最佳实践

```javascript
// 1. Slice设计
// ✅ 好的设计：按功能分片
// features/user/userSlice.ts
// features/cart/cartSlice.ts
// features/product/productSlice.ts

// ❌ 不好的设计：按类型分片
// slices/actionsSlice.ts
// slices/selectorsSlice.ts
// slices/reducersSlice.ts

// 2. 异步处理
// ✅ 好的设计：使用createAsyncThunk
export const fetchUser = createAsyncThunk(
  'user/fetchUser',
  async (userId: number) => {
    const response = await fetch(`/api/users/${userId}`);
    return await response.json();
  }
);

// ❌ 不好的设计：手动dispatch
export const fetchUserOld = (userId: number) => async (dispatch) => {
  dispatch({ type: 'user/fetchUser/pending' });
  try {
    const response = await fetch(`/api/users/${userId}`);
    const user = await response.json();
    dispatch({ type: 'user/fetchUser/fulfilled', payload: user });
  } catch (error) {
    dispatch({ type: 'user/fetchUser/rejected', error: error.message });
  }
};

// 3. 状态更新
// ✅ 好的设计：Immer可变语法
updateUser: (state, action) => {
  state.user.name = action.payload.name;  // 直接修改
}

// ❌ 不好的设计：手动不可变更新
updateUserOld: (state, action) => ({
  ...state,
  user: {
    ...state.user,
    name: action.payload.name,
  },
});

// 4. 类型安全
// ✅ 好的设计：完整类型定义
interface UserState {
  user: User | null;
  loading: boolean;
}

// ❌ 不好的设计：使用any
interface UserState {
  user: any;
  loading: any;
}
```

---

## 4. Context API与useReducer

### 4.1 Context API基础

```javascript
// contexts/UserContext.tsx
import React, { createContext, useContext, useState, ReactNode } from 'react';

// 定义类型
interface User {
  id: number;
  name: string;
  email: string;
}

interface UserContextType {
  user: User | null;
  setUser: (user: User) => void;
  clearUser: () => void;
}

// 创建Context
const UserContext = createContext<UserContextType | undefined>(undefined);

// Provider组件
export function UserProvider({ children }: { children: ReactNode }) {
  const [user, setUser] = useState<User | null>(null);

  const clearUser = () => setUser(null);

  return (
    <UserContext.Provider value={{ user, setUser, clearUser }}>
      {children}
    </UserContext.Provider>
  );
}

// 自定义Hook
export function useUser() {
  const context = useContext(UserContext);
  if (context === undefined) {
    throw new Error('useUser必须在UserProvider中使用');
  }
  return context;
}

// 在App中使用
function App() {
  return (
    <UserProvider>
      <Router>
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/profile" element={<Profile />} />
        </Routes>
      </Router>
    </UserProvider>
  );
}

// 在组件中使用
function Profile() {
  const { user, setUser, clearUser } = useUser();

  return (
    <div>
      <h1>{user?.name}</h1>
      <button onClick={() => clearUser()}>退出登录</button>
    </div>
  );
}
```

### 4.2 useReducer深度解析

```javascript
// 定义Action类型
type CounterAction =
  | { type: 'INCREMENT'; payload?: number }
  | { type: 'DECREMENT'; payload?: number }
  | { type: 'RESET' }
  | { type: 'SET_VALUE'; payload: number };

// 定义Reducer
function counterReducer(state: number, action: CounterAction): number {
  switch (action.type) {
    case 'INCREMENT':
      return state + (action.payload || 1);
    case 'DECREMENT':
      return state - (action.payload || 1);
    case 'RESET':
      return 0;
    case 'SET_VALUE':
      return action.payload;
    default:
      return state;
  }
}

// 使用useReducer
function Counter() {
  const [count, dispatch] = useReducer(counterReducer, 0);

  return (
    <div>
      <h1>计数: {count}</h1>
      <button onClick={() => dispatch({ type: 'INCREMENT' })}>
        增加
      </button>
      <button onClick={() => dispatch({ type: 'DECREMENT' })}>
        减少
      </button>
      <button onClick={() => dispatch({ type: 'RESET' })}>
        重置
      </button>
      <button onClick={() => dispatch({ type: 'SET_VALUE', payload: 10 })}>
        设置为10
      </button>
    </div>
  );
}

// 复杂状态管理
interface Todo {
  id: number;
  text: string;
  completed: boolean;
}

interface TodoState {
  todos: Todo[];
  filter: 'all' | 'active' | 'completed';
}

type TodoAction =
  | { type: 'ADD_TODO'; payload: string }
  | { type: 'TOGGLE_TODO'; payload: number }
  | { type: 'DELETE_TODO'; payload: number }
  | { type: 'SET_FILTER'; payload: TodoState['filter'] };

function todoReducer(state: TodoState, action: TodoAction): TodoState {
  switch (action.type) {
    case 'ADD_TODO':
      return {
        ...state,
        todos: [
          ...state.todos,
          {
            id: Date.now(),
            text: action.payload,
            completed: false,
          },
        ],
      };
    case 'TOGGLE_TODO':
      return {
        ...state,
        todos: state.todos.map((todo) =>
          todo.id === action.payload
            ? { ...todo, completed: !todo.completed }
            : todo
        ),
      };
    case 'DELETE_TODO':
      return {
        ...state,
        todos: state.todos.filter((todo) => todo.id !== action.payload),
      };
    case 'SET_FILTER':
      return {
        ...state,
        filter: action.payload,
      };
    default:
      return state;
  }
}

function TodoApp() {
  const [state, dispatch] = useReducer(todoReducer, {
    todos: [],
    filter: 'all',
  });

  const filteredTodos = state.todos.filter((todo) => {
    if (state.filter === 'active') return !todo.completed;
    if (state.filter === 'completed') return todo.completed;
    return true;
  });

  return (
    <div>
      <input
        type="text"
        placeholder="添加新任务"
        onKeyPress={(e) => {
          if (e.key === 'Enter') {
            dispatch({ type: 'ADD_TODO', payload: e.currentTarget.value });
            e.currentTarget.value = '';
          }
        }}
      />
      <div>
        <button onClick={() => dispatch({ type: 'SET_FILTER', payload: 'all' })}>
          全部
        </button>
        <button onClick={() => dispatch({ type: 'SET_FILTER', payload: 'active' })}>
          进行中
        </button>
        <button
          onClick={() => dispatch({ type: 'SET_FILTER', payload: 'completed' })}
        >
          已完成
        </button>
      </div>
      <ul>
        {filteredTodos.map((todo) => (
          <li key={todo.id}>
            <input
              type="checkbox"
              checked={todo.completed}
              onChange={() => dispatch({ type: 'TOGGLE_TODO', payload: todo.id })}
            />
            <span>{todo.text}</span>
            <button
              onClick={() => dispatch({ type: 'DELETE_TODO', payload: todo.id })}
            >
              删除
            </button>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

### 4.3 Context + useReducer组合

```javascript
// contexts/TodoContext.tsx
import React, { createContext, useContext, useReducer, ReactNode } from 'react';

// 定义类型
interface Todo {
  id: number;
  text: string;
  completed: boolean;
}

interface TodoState {
  todos: Todo[];
  filter: 'all' | 'active' | 'completed';
}

type TodoAction =
  | { type: 'ADD_TODO'; payload: string }
  | { type: 'TOGGLE_TODO'; payload: number }
  | { type: 'DELETE_TODO'; payload: number }
  | { type: 'SET_FILTER'; payload: TodoState['filter'] };

interface TodoContextType {
  state: TodoState;
  addTodo: (text: string) => void;
  toggleTodo: (id: number) => void;
  deleteTodo: (id: number) => void;
  setFilter: (filter: TodoState['filter']) => void;
}

const TodoContext = createContext<TodoContextType | undefined>(undefined);

function todoReducer(state: TodoState, action: TodoAction): TodoState {
  switch (action.type) {
    case 'ADD_TODO':
      return {
        ...state,
        todos: [
          ...state.todos,
          {
            id: Date.now(),
            text: action.payload,
            completed: false,
          },
        ],
      };
    case 'TOGGLE_TODO':
      return {
        ...state,
        todos: state.todos.map((todo) =>
          todo.id === action.payload
            ? { ...todo, completed: !todo.completed }
            : todo
        ),
      };
    case 'DELETE_TODO':
      return {
        ...state,
        todos: state.todos.filter((todo) => todo.id !== action.payload),
      };
    case 'SET_FILTER':
      return {
        ...state,
        filter: action.payload,
      };
    default:
      return state;
  }
}

export function TodoProvider({ children }: { children: ReactNode }) {
  const [state, dispatch] = useReducer(todoReducer, {
    todos: [],
    filter: 'all',
  });

  const addTodo = (text: string) => {
    dispatch({ type: 'ADD_TODO', payload: text });
  };

  const toggleTodo = (id: number) => {
    dispatch({ type: 'TOGGLE_TODO', payload: id });
  };

  const deleteTodo = (id: number) => {
    dispatch({ type: 'DELETE_TODO', payload: id });
  };

  const setFilter = (filter: TodoState['filter']) => {
    dispatch({ type: 'SET_FILTER', payload: filter });
  };

  return (
    <TodoContext.Provider
      value={{ state, addTodo, toggleTodo, deleteTodo, setFilter }}
    >
      {children}
    </TodoContext.Provider>
  );
}

export function useTodo() {
  const context = useContext(TodoContext);
  if (context === undefined) {
    throw new Error('useTodo必须在TodoProvider中使用');
  }
  return context;
}

// 性能优化：拆分Context
// contexts/TodoStateContext.tsx
const TodoStateContext = createContext<TodoState | undefined>(undefined);

export function TodoStateProvider({ children }: { children: ReactNode }) {
  const [state] = useReducer(todoReducer, {
    todos: [],
    filter: 'all',
  });

  return (
    <TodoStateContext.Provider value={state}>
      {children}
    </TodoStateContext.Provider>
  );
}

export function useTodoState() {
  const context = useContext(TodoStateContext);
  if (context === undefined) {
    throw new Error('useTodoState必须在TodoStateProvider中使用');
  }
  return context;
}

// contexts/TodoActionsContext.tsx
const TodoActionsContext = createContext<{
  addTodo: (text: string) => void;
  toggleTodo: (id: number) => void;
  deleteTodo: (id: number) => void;
  setFilter: (filter: TodoState['filter']) => void;
} | undefined>(undefined);

export function TodoActionsProvider({ children }: { children: ReactNode }) {
  const [state, dispatch] = useReducer(todoReducer, {
    todos: [],
    filter: 'all',
  });

  const addTodo = (text: string) => {
    dispatch({ type: 'ADD_TODO', payload: text });
  };

  const toggleTodo = (id: number) => {
    dispatch({ type: 'TOGGLE_TODO', payload: id });
  };

  const deleteTodo = (id: number) => {
    dispatch({ type: 'DELETE_TODO', payload: id });
  };

  const setFilter = (filter: TodoState['filter']) => {
    dispatch({ type: 'SET_FILTER', payload: filter });
  };

  return (
    <TodoActionsContext.Provider
      value={{ addTodo, toggleTodo, deleteTodo, setFilter }}
    >
      <TodoStateContext.Provider value={state}>
        {children}
      </TodoStateContext.Provider>
    </TodoActionsContext.Provider>
  );
}

export function useTodoActions() {
  const context = useContext(TodoActionsContext);
  if (context === undefined) {
    throw new Error('useTodoActions必须在TodoActionsProvider中使用');
  }
  return context;
}
```

---

## 5. Recoil与Jotai

### 5.1 Recoil基础

```javascript
// atoms.ts
import { atom, selector } from 'recoil';

// 定义Atom（状态单元）
export const textState = atom({
  key: 'textState',  // 唯一标识
  default: '',       // 默认值
});

export const charCountState = selector({
  key: 'charCountState',
  get: ({ get }) => {
    const text = get(textState);
    return text.length;
  },
});

// 复杂Atom
export const todoListState = atom({
  key: 'todoListState',
  default: [],
});

export const todoListFilterState = atom({
  key: 'todoListFilterState',
  default: 'Show All',
});

// Selector（衍生状态）
export const filteredTodoListState = selector({
  key: 'filteredTodoListState',
  get: ({ get }) => {
    const filter = get(todoListFilterState);
    const list = get(todoListState);

    switch (filter) {
      case 'Show Completed':
        return list.filter((item) => item.isComplete);
      case 'Show Active':
        return list.filter((item) => !item.isComplete);
      default:
        return list;
    }
  },
});

export const todoListStatsState = selector({
  key: 'todoListStatsState',
  get: ({ get }) => {
    const todoList = get(todoListState);
    const totalNum = todoList.length;
    const totalCompletedNum = todoList.filter((item) => item.isComplete).length;
    const totalUncompletedNum = totalNum - totalCompletedNum;
    const percentCompleted = totalNum === 0
      ? 0
      : totalCompletedNum / totalNum;

    return {
      totalNum,
      totalCompletedNum,
      totalUncompletedNum,
      percentCompleted: Math.round(percentCompleted * 100),
    };
  },
});

// Async Atom
export const currentUserNameQuery = selector({
  key: 'currentUserNameQuery',
  get: async ({ get }) => {
    const response = await fetch('/api/users/current');
    return response.json();
  },
});

// 在组件中使用
import { useRecoilState, useRecoilValue, useSetRecoilState } from 'recoil';
import { textState, charCountState, todoListState } from './atoms';

function CharacterCounter() {
  const [text, setText] = useRecoilState(textState);
  const count = useRecoilValue(charCountState);

  return (
    <div>
      <input
        type="text"
        value={text}
        onChange={(e) => setText(e.target.value)}
      />
      <p>字符数: {count}</p>
    </div>
  );
}

function TodoItem({ item }) {
  const [todoList, setTodoList] = useRecoilState(todoListState);
  const index = todoList.findIndex((todo) => todo.id === item.id);

  const toggleItem = () => {
    const newList = replaceItemAtIndex(todoList, index, {
      ...item,
      isComplete: !item.isComplete,
    });
    setTodoList(newList);
  };

  const deleteItem = () => {
    const newList = removeItemAtIndex(todoList, index);
    setTodoList(newList);
  };

  return (
    <div>
      <input
        type="checkbox"
        checked={item.isComplete}
        onChange={toggleItem}
      />
      <span>{item.text}</span>
      <button onClick={deleteItem}>删除</button>
    </div>
  );
}
```

### 5.2 Jotai基础

```javascript
// atoms.ts
import { atom, useAtom } from 'jotai';

// 基础Atom
export const countAtom = atom(0);

// 只读Atom
export const doubleCountAtom = atom((get) => get(countAtom) * 2);

// 读写Atom
export const textAtom = atom('hello');

// 只写Atom
export const uppercaseAtom = atom(
  (get) => get(textAtom).toUpperCase(),
  (get, set, arg: string) => {
    set(textAtom, arg.toUpperCase());
  }
);

// 派生Atom
export const isValidAtom = atom((get) => {
  const text = get(textAtom);
  return text.length > 0;
});

// Async Atom
export const userAtom = atom(async (get) => {
  const response = await fetch('/api/users/current');
  return response.json();
});

// 在组件中使用
function Counter() {
  const [count, setCount] = useAtom(countAtom);

  return (
    <div>
      <p>计数: {count}</p>
      <button onClick={() => setCount(count + 1)}>增加</button>
    </div>
  );
}

function DoubleCounter() {
  const [doubleCount] = useAtom(doubleCountAtom);

  return <div>双倍计数: {doubleCount}</div>;
}

// Jotai Utils
import { atomWithReset, atomWithImmer, splitAtom } from 'jotai/utils';

// reset功能的Atom
export const countWithResetAtom = atomWithReset(0);

// Immer Atom（不可变更新）
export const todosAtom = atomWithImmer([]);

function TodoList() {
  const [todos, setTodos] = useAtom(todosAtom);

  const addTodo = (text) => {
    setTodos((draft) => {
      draft.push({ id: Date.now(), text, completed: false });
    });
  };

  const toggleTodo = (id) => {
    setTodos((draft) => {
      const todo = draft.find((t) => t.id === id);
      if (todo) {
        todo.completed = !todo.completed;
      }
    });
  };

  return (
    <div>
      <input
        type="text"
        onKeyPress={(e) => {
          if (e.key === 'Enter') {
            addTodo(e.currentTarget.value);
            e.currentTarget.value = '';
          }
        }}
      />
      <ul>
        {todos.map((todo) => (
          <li key={todo.id}>
            <input
              type="checkbox"
              checked={todo.completed}
              onChange={() => toggleTodo(todo.id)}
            />
            <span>{todo.text}</span>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

### 5.3 Recoil vs Jotai对比

| 特性 | Recoil | Jotai |
|------|--------|-------|
| **学习曲线** | 🟡 中等 | 🟢 简单 |
| **Bundle大小** | 📦 较大 | 📦 小 |
| **TypeScript** | ✅ 友好 | ✅ 优秀 |
| **性能** | ✅ 好 | ✅ 优秀 |
| **生态系统** | 🟢 丰富 | 🟡 成长中 |

---

## 6. 状态管理最佳实践

### 6.1 性能优化

```javascript
// Zustand性能优化
import { shallow } from 'zustand/shallow';

// ✅ 使用shallow避免不必要的重渲染
const { name, email } = useUserStore(
  (state) => ({ name: state.user?.name, email: state.user?.email }),
  shallow
);

// ❌ 每次更新都重渲染
const { user } = useUserStore();

// React Redux性能优化
import { useSelector } from 'react-redux';

// ✅ 使用记忆化选择器
const userName = useSelector(selectUserName);

// ❌ 每次更新都重渲染
const user = useSelector((state) => state.user);

// Context性能优化
// ✅ 拆分Context
const UserStateContext = createContext();
const UserActionsContext = createContext();

function UserProfile() {
  const { name } = useContext(UserStateContext);  // 只订阅状态
  const { setName } = useContext(UserActionsContext);  // 只订阅actions

  return <div>{name}</div>;
}
```

### 6.2 状态设计原则

```javascript
// 1. 单一数据源
// ✅ 好的设计
const user = {
  id: 1,
  name: '张三',
  email: 'zhangsan@example.com',
};

// ❌ 不好的设计：重复数据
const userId = 1;
const userName = '张三';
const userEmail = 'zhangsan@example.com';

// 2. 不可变数据
// ✅ 好的设计：返回新对象
const newState = {
  ...state,
  user: {
    ...state.user,
    name: '新名字',
  },
};

// ❌ 不好的设计：直接修改
state.user.name = '新名字';

// 3. 归一化数据
// ✅ 好的设计：归一化
const normalized = {
  users: {
    1: { id: 1, name: '张三' },
    2: { id: 2, name: '李四' },
  },
  posts: {
    1: { id: 1, title: '文章1', author: 1 },
    2: { id: 2, title: '文章2', author: 2 },
  },
};

// ❌ 不好的设计：嵌套数据
const nested = {
  posts: [
    {
      id: 1,
      title: '文章1',
      author: {
        id: 1,
        name: '张三',
      },
    },
  ],
};

// 4. 最小化状态
// ✅ 好的设计：只存储必要数据
const [dob, setDob] = useState('1990-01-01');
const age = useMemo(() => calculateAge(dob), [dob]);

// ❌ 不好的设计：存储衍生状态
const [dob, setDob] = useState('1990-01-01');
const [age, setAge] = useState(34);  // 冗余状态
```

### 6.3 状态管理选择指南

```javascript
// 状态管理决策树
function selectStateManagement(requirements) {
  // 1. 组件内部状态
  if (requirements.scope === 'component') {
    return 'useState';
  }

  // 2. 简单跨组件共享
  if (
    requirements.scope === 'global' &&
    requirements.complexity === 'low' &&
    !requirements.persistence
  ) {
    return 'Context API';
  }

  // 3. 需要持久化
  if (requirements.persistence) {
    if (requirements.complexity === 'low') {
      return 'Zustand';
    }
    return 'Redux Toolkit';
  }

  // 4. 复杂业务逻辑
  if (
    requirements.complexity === 'high' ||
    requirements.middleware ||
    requirements.timeTravel
  ) {
    return 'Redux Toolkit';
  }

  // 5. 原子化状态
  if (requirements.atomicState || requirements.atomFamily) {
    return 'Recoil';
  }

  // 6. 默认选择
  return 'Zustand';
}
```

---

## 参考资源

- [Zustand官方文档](https://github.com/pmndrs/zustand)
- [Redux Toolkit官方文档](https://redux-toolkit.js.org/)
- [Recoil官方文档](https://recoiljs.org/)
- [Jotai官方文档](https://jotai.org/)
- [React Context](https://react.dev/reference/react/useContext)

---

*本文档持续更新，最后更新于2026年3月*