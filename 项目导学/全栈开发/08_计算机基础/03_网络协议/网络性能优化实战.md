# 网络性能优化实战

## 目录

1. [网络性能基础](#1-网络性能基础)
2. [HTTP协议优化](#2-http协议优化)
3. [资源加载优化](#3-资源加载优化)
4. [缓存策略实战](#4-缓存策略实战)
5. [CDN与边缘计算](#5-cdn与边缘计算)
6. [API性能优化](#6-api性能优化)
7. [移动端网络优化](#7-移动端网络优化)
8. [面试高频问题](#8-面试高频问题)

---

## 1. 网络性能基础

### 1.1 网络性能指标

网络性能直接影响用户体验，以下是关键指标：

```javascript
// 网络性能核心指标

const networkMetrics = {
  // 延迟指标
  latency: {
    ttfb: {
      全称: 'Time to First Byte',
      描述: '请求发出到收到首字节的时间',
      理想值: '< 200ms',
      测量方法: 'performance.timing.responseStart - performance.timing.requestStart',
    },
    rtt: {
      全称: 'Round Trip Time',
      描述: '请求到响应往返时间',
      影响因素: ['物理距离', '网络拥塞', '服务器性能'],
    },
  },

  // 带宽指标
  bandwidth: {
    下载速度: 'Mbps（兆比特每秒）',
    上传速度: 'Mbps',
    瓶颈: ['用户带宽', '服务器带宽', 'CDN速度'],
  },

  // 资源加载指标
  resourceLoad: {
    dnsLookup: 'DNS查询时间',
    tcpConnection: 'TCP连接时间',
    sslHandshake: 'SSL握手时间',
    requestTime: '请求发送时间',
    responseTime: '响应传输时间',
    totalTime: '总加载时间',
  },

  // 感知性能指标
  perceivedPerformance: {
    开始渲染: '首次绘制时间',
    有效渲染: '首次有效绘制时间',
    可交互: 'TTI（可交互时间）',
    完全加载: '完全加载时间',
  },
};

// 网络性能监控
function monitorNetworkPerformance() {
  const resources = performance.getEntriesByType('resource');

  const summary = resources.reduce((acc, resource) => {
    const type = resource.initiatorType;
    if (!acc[type]) {
      acc[type] = { count: 0, totalDuration: 0, totalSize: 0 };
    }
    acc[type].count++;
    acc[type].totalDuration += resource.duration;
    acc[type].totalSize += resource.transferSize || 0;
    return acc;
  }, {});

  Object.entries(summary).forEach(([type, data]) => {
    console.log(`${type}:`, {
      请求数: data.count,
      平均耗时: (data.totalDuration / data.count).toFixed(2) + 'ms',
      总大小: (data.totalSize / 1024).toFixed(2) + 'KB',
    });
  });
}
```

### 1.2 网络请求阶段分析

```javascript
// 详细分析网络请求各阶段

function analyzeRequestStages() {
  const timing = performance.getEntriesByType('navigation')[0];

  // 各阶段详解
  const stages = {
    // 1. 重定向阶段
    redirect: {
      时间: timing.redirectEnd - timing.redirectStart,
      优化: '避免不必要的重定向',
    },

    // 2. DNS查询阶段
    dns: {
      时间: timing.domainLookupEnd - timing.domainLookupStart,
      优化: '使用dns-prefetch预解析',
    },

    // 3. TCP连接阶段
    tcp: {
      时间: timing.connectEnd - timing.connectStart,
      优化: '使用preconnect预连接',
    },

    // 4. SSL握手阶段
    ssl: {
      时间: timing.secureConnectionStart > 0
        ? timing.connectEnd - timing.secureConnectionStart
        : 0,
      优化: '使用TLS 1.3减少握手时间',
    },

    // 5. 请求阶段
    request: {
      时间: timing.responseStart - timing.requestStart,
      优化: '优化服务器处理速度',
    },

    // 6. 响应阶段
    response: {
      时间: timing.responseEnd - timing.responseStart,
      优化: '压缩资源、使用CDN',
    },

    // 7. 内容处理阶段
    processing: {
      DOM解析: timing.domInteractive - timing.responseEnd,
      DOM完成: timing.domComplete - timing.domInteractive,
    },

    // 8. 加载事件阶段
    loadEvent: {
      时间: timing.loadEventEnd - timing.loadEventStart,
      优化: '减少同步脚本、延迟加载',
    },
  };

  return stages;
}

// 瀑布图分析
function analyzeWaterfall() {
  const resources = performance.getEntriesByType('resource');

  // 按开始时间排序
  const sorted = resources
    .map(r => ({
      name: r.name.split('/').pop(),
      type: r.initiatorType,
      start: r.startTime,
      duration: r.duration,
      size: r.transferSize,
      // 各阶段
      dns: r.domainLookupEnd - r.domainLookupStart,
      tcp: r.connectEnd - r.connectStart,
      ttfb: r.responseStart - r.requestStart,
      download: r.responseEnd - r.responseStart,
    }))
    .sort((a, b) => a.start - b.start);

  return sorted;
}
```

---

## 2. HTTP协议优化

### 2.1 HTTP/2优化配置

```javascript
// HTTP/2优化实践

// 1. 启用HTTP/2
const http2Config = {
  // Nginx配置示例
  nginx: `
    server {
        listen 443 ssl http2;

        ssl_certificate /path/to/cert.pem;
        ssl_certificate_key /path/to/key.pem;

        # 启用HTTP/2
        http2 on;

        # TLS配置
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;
    }
  `,

  // Node.js配置示例
  node: `
    const http2 = require('http2');
    const fs = require('fs');

    const server = http2.createSecureServer({
      cert: fs.readFileSync('cert.pem'),
      key: fs.readFileSync('key.pem'),
    }, (req, res) => {
      res.writeHead(200, { 'content-type': 'text/plain' });
      res.end('Hello HTTP/2!');
    });
  `,
};

// 2. HTTP/2服务器推送
const serverPush = {
  // Nginx配置
  nginx: `
    location / {
        # 预加载关键资源
        http2_push /css/main.css;
        http2_push /js/app.js;
        http2_push /fonts/font.woff2;
    }
  `,

  // Link头方式
  linkHeader: `
    < HTTP/2 200
    < content-type: text/html
    < link: </css/main.css>; rel=preload; as=style
    < link: </js/app.js>; rel=preload; as=script
    < link: </fonts/font.woff2>; rel=preload; as=font; crossorigin
  `,
};

// 3. HTTP/2性能监控
function monitorHTTP2() {
  const resources = performance.getEntriesByType('resource');

  // 检查协议类型（需要在DevTools中查看）
  const http2Resources = resources.filter(r => {
    // Protocol列在DevTools中显示为h2
    return r.nextHopProtocol === 'h2' || r.nextHopProtocol === 'http/2';
  });

  const http1Resources = resources.filter(r =>
    r.nextHopProtocol === 'http/1.1'
  );

  console.log({
    http2请求数: http2Resources.length,
    http1请求数: http1Resources.length,
    http2节省时间: estimateTimeSavings(http2Resources, http1Resources),
  });
}
```

### 2.2 HTTP/3与QUIC

```javascript
// HTTP/3优化实践

// 1. 启用HTTP/3
const http3Config = {
  // Nginx配置（需要nginx-quic）
  nginx: `
    server {
        listen 443 ssl;
        listen 443 quic reuseport;

        # HTTP/3配置
        http3 on;
        quic_gso on;
        quic_host_key /path/to/key.pem;

        ssl_protocols TLSv1.3;
    }
  `,

  // Cloudflare（自动启用HTTP/3）
  cloudflare: '在Cloudflare控制台启用HTTP/3',
};

// 2. QUIC优势
const quicAdvantages = {
  连接建立: {
    http11: '1 RTT（TCP）+ 1-2 RTT（TLS）= 2-3 RTT',
    http2: '1 RTT（TCP）+ 1-2 RTT（TLS）= 2-3 RTT',
    http3: '0-1 RTT（首次连接0-RTT或1-RTT）',
  },

  丢包处理: {
    http2: 'TCP层丢包阻塞所有流',
    http3: 'QUIC层丢包只影响单个流',
  },

  连接迁移: {
    http2: '网络切换需要重新建立连接',
    http3: '使用连接ID保持连接',
  },
};

// 3. HTTP/3检测
function detectHTTP3Support() {
  // 检查连接协议
  const connection = performance.getEntriesByType('resource')[0];

  // 通过Performance API无法直接检测
  // 需要使用Web API
  if (navigator.connection) {
    const effectiveType = navigator.connection.effectiveType;
    console.log('网络类型:', effectiveType);

    // 5G网络下更可能使用HTTP/3
    if (effectiveType === '5g') {
      console.log('可能使用HTTP/3');
    }
  }
}
```

### 2.3 TLS优化

```javascript
// TLS优化配置

const tlsOptimization = {
  // 1. 使用TLS 1.3
  tls13: {
    优点: [
      '握手时间从2-RTT减少到1-RTT',
      '移除不安全的加密算法',
      '更好的前向保密',
    ],
    配置: `
      ssl_protocols TLSv1.3;
    `,
  },

  // 2. 会话复用
  sessionResumption: {
    会话票据: `
      ssl_session_cache shared:SSL:10m;
      ssl_session_tickets on;
      ssl_session_timeout 1d;
    `,
    会话ID: `
      ssl_session_cache shared:SSL:10m;
      ssl_session_timeout 1d;
    `,
  },

  // 3. OCSP stapling
  ocspStapling: `
    ssl_stapling on;
    ssl_stapling_verify on;
    resolver 8.8.8.8 8.8.4.4 valid=300s;
    resolver_timeout 5s;
  `,

  // 4. 证书优化
  certificateOptimization: {
    证书链: '只包含必要的中间证书',
    证书格式: '使用PEM格式，避免过长的证书链',
    SNI: '支持SNI，减少IP地址使用',
  },
};
```

---

## 3. 资源加载优化

### 3.1 预加载与预连接

```html
<!-- 预加载技术详解 -->

<!-- 1. preconnect - 预建立连接 -->
<head>
  <!-- 预连接关键域名 -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="preconnect" href="https://cdn.example.com">

  <!-- 预连接API域名 -->
  <link rel="preconnect" href="https://api.example.com">

  <!-- 预连接WebSocket -->
  <link rel="preconnect" href="wss://realtime.example.com">
</head>

<!-- 2. dns-prefetch - DNS预解析 -->
<head>
  <!-- 旧浏览器兼容 -->
  <link rel="dns-prefetch" href="https://fonts.googleapis.com">
  <link rel="dns-prefetch" href="https://cdn.example.com">
</head>

<!-- 3. prefetch - 预获取资源（未来可能使用） -->
<head>
  <!-- 预获取下一个页面需要的资源 -->
  <link rel="prefetch" href="/next-page.html">
  <link rel="prefetch" href="/js/next-page.js">
  <link rel="prefetch" href="/css/next-page.css">

  <!-- 预获取图片 -->
  <link rel="prefetch" href="/images/hero.webp" as="image">
</head>

<!-- 4. prerender - 预渲染页面 -->
<head>
  <!-- 后台渲染整个页面 -->
  <link rel="prerender" href="/next-page.html">
</head>

<!-- 5. preload - 预加载当前页面需要的资源 -->
<head>
  <!-- 预加载关键CSS -->
  <link rel="preload" href="/css/main.css" as="style">

  <!-- 预加载关键JS -->
  <link rel="preload" href="/js/app.js" as="script">

  <!-- 预加载字体 -->
  <link rel="preload" href="/fonts/main.woff2" as="font" type="font/woff2" crossorigin>

  <!-- 预加载LCP图片 -->
  <link rel="preload" href="/images/hero.webp" as="image" fetchpriority="high">

  <!-- 预加载JSON数据 -->
  <link rel="preload" href="/api/config.json" as="fetch" crossorigin>
</head>

<!-- 6. 模块预加载 -->
<head>
  <!-- 预加载ES模块 -->
  <link rel="modulepreload" href="/js/lib.mjs">
  <link rel="modulepreload" href="/js/app.mjs">
</head>
```

```javascript
// 动态预加载
function dynamicPreload() {
  // 基于用户行为的预加载
  const preloadLink = document.createElement('link');
  preloadLink.rel = 'preload';
  preloadLink.as = 'image';
  preloadLink.href = '/images/hovered.webp';
  document.head.appendChild(preloadLink);
}

// 预测性预加载
function predictivePreload() {
  // 鼠标悬停时预加载
  document.querySelectorAll('a').forEach(link => {
    link.addEventListener('mouseenter', () => {
      const url = link.href;
      const prefetchLink = document.createElement('link');
      prefetchLink.rel = 'prefetch';
      prefetchLink.href = url;
      document.head.appendChild(prefetchLink);
    }, { passive: true });
  });
}
```

### 3.2 资源加载优先级

```javascript
// 资源加载优先级策略

const priorityStrategy = {
  // 高优先级资源（立即加载）
  high: [
    'HTML文档',
    '关键CSS',
    '关键JS（同步）',
    '字体（文字渲染）',
    'LCP图片',
  ],

  // 中优先级资源（尽快加载）
  medium: [
    '首屏图片',
    '非关键CSS',
    '预连接',
  ],

  // 低优先级资源（延迟加载）
  low: [
    '非关键JS',
    '非首屏图片',
    '预加载资源',
    '预获取资源',
  ],

  // 最低优先级
  lowest: [
    '分析脚本',
    '广告脚本',
    '社交媒体插件',
  ],
};

// 控制资源加载优先级
function controlResourcePriority() {
  // 使用fetchpriority属性
  const highPriorityImage = `
    <img src="hero.webp" fetchpriority="high" alt="Hero">
    <img src="other.webp" fetchpriority="low" alt="Other">
  `;

  // 使用link标签的as属性
  const preloadWithPriority = `
    <link rel="preload" href="critical.js" as="script" importance="high">
  `;

  // JavaScript控制
  const link = document.createElement('link');
  link.rel = 'preload';
  link.href = '/images/hero.webp';
  link.as = 'image';
  link.fetchPriority = 'high';
  document.head.appendChild(link);
}

// Resource Hints性能测试
function testResourceHints() {
  const tests = {
    // 无优化
    baseline: measureLoadTime(),

    // 预连接
    withPreconnect: () => {
      addPreconnectHints();
      return measureLoadTime();
    },

    // 预加载
    withPreload: () => {
      addPreloadHints();
      return measureLoadTime();
    },

    // 组合优化
    combined: () => {
      addAllHints();
      return measureLoadTime();
    },
  };

  return tests;
}
```

### 3.3 资源加载策略

```javascript
// 资源加载策略

// 1. 关键资源内联
function inlineCriticalResources() {
  const criticalCSS = `
    .header { background: #fff; }
    .hero { min-height: 100vh; }
    .footer { margin-top: 40px; }
  `;

  // 内联到HTML中
  document.write(`<style>${criticalCSS}</style>`);
}

// 2. 非关键资源延迟加载
function lazyLoadNonCritical() {
  // 图片懒加载
  const images = document.querySelectorAll('img[data-src]');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const img = entry.target;
        img.src = img.dataset.src;
        img.removeAttribute('data-src');
        observer.unobserve(img);
      }
    });
  });

  images.forEach(img => observer.observe(img));

  // JS延迟加载
  const loadScript = (src) => {
    return new Promise((resolve, reject) => {
      const script = document.createElement('script');
      script.src = src;
      script.onload = resolve;
      script.onerror = reject;
      document.body.appendChild(script);
    });
  };

  // 首屏渲染后加载非关键脚本
  window.addEventListener('load', () => {
    loadScript('/js/analytics.js');
    loadScript('/js/chatbot.js');
  });
}

// 3. CSS加载策略
function loadCSSStrategically() {
  // 关键CSS同步加载
  // 非关键CSS异步加载
  const loadCSSTest = `
    <!-- 关键CSS -->
    <link rel="stylesheet" href="/css/critical.css">

    <!-- 非关键CSS（不阻塞渲染） -->
    <link rel="stylesheet" href="/css/non-critical.css" media="print" onload="this.media='all'">

    <!-- 备用方案 -->
    <noscript>
      <link rel="stylesheet" href="/css/non-critical.css">
    </noscript>
  `;
}

// 4. 字体加载策略
function optimizeFontLoading() {
  // 使用font-display
  const fontFace = `
    @font-face {
      font-family: 'CustomFont';
      src: url('/fonts/custom.woff2') format('woff2');
      font-display: swap;
    }
  `;

  // 预加载字体
  const preloadFont = `
    <link rel="preload" href="/fonts/main.woff2" as="font" crossorigin type="font/woff2">
  `;

  // 使用CSS Font Loading API
  document.fonts.ready.then(() => {
    console.log('字体加载完成');
  });
}
```

---

## 4. 缓存策略实战

### 4.1 HTTP缓存配置

```javascript
// HTTP缓存策略配置

// 1. 强缓存配置
const cacheControlConfig = {
  // 不缓存
  noCache: {
    'Cache-Control': 'no-cache, no-store, must-revalidate',
    'Pragma': 'no-cache',
    'Expires': '0',
  },

  // 私有缓存（浏览器）
  private: {
    'Cache-Control': 'private, max-age=3600',
  },

  // 公共缓存（CDN等）
  public: {
    'Cache-Control': 'public, max-age=3600, immutable',
  },

  // 长期缓存（用于版本化资源）
  immutable: {
    'Cache-Control': 'public, max-age=31536000, immutable',
  },
};

// 2. 协商缓存配置
const etagConfig = {
  // 生成ETag
  generateETag: (content) => {
    const crypto = require('crypto');
    return crypto.createHash('md5').update(content).digest('hex');
  },

  // 验证ETag
  validateETag: (request, etag) => {
    const ifNoneMatch = request.headers['if-none-match'];
    return ifNoneMatch === etag;
  },

  // Last-Modified
  lastModified: {
    设置: 'Last-Modified: ' + new Date().toUTCString(),
    验证: 'If-Modified-Since: <date>',
  },
};

// 3. 缓存策略选择器
function selectCacheStrategy(resourceType) {
  const strategies = {
    // HTML：每次验证
    html: {
      'Cache-Control': 'no-cache, no-store, must-revalidate',
      'Pragma': 'no-cache',
    },

    // CSS/JS：长期缓存
    static: {
      'Cache-Control': 'public, max-age=31536000, immutable',
    },

    // 图片：长期缓存
    images: {
      'Cache-Control': 'public, max-age=31536000',
    },

    // API响应：根据数据时效
    api: {
      realtime: { 'Cache-Control': 'no-cache' },
      hourly: { 'Cache-Control': 'max-age=3600' },
      daily: { 'Cache-Control': 'max-age=86400' },
    },

    // 字体：长期缓存
    fonts: {
      'Cache-Control': 'public, max-age=31536000, immutable',
    },
  };

  return strategies[resourceType] || strategies.static;
}
```

### 4.2 Service Worker缓存

```javascript
// Service Worker缓存策略

// 1. 缓存策略类型
const cacheStrategies = {
  // Cache First（缓存优先）
  cacheFirst: `
    self.addEventListener('fetch', event => {
      event.respondWith(
        caches.match(event.request)
          .then(response => response || fetch(event.request))
      );
    });
  `,

  // Network First（网络优先）
  networkFirst: `
    self.addEventListener('fetch', event => {
      event.respondWith(
        fetch(event.request)
          .then(response => {
            const clone = response.clone();
            caches.open('cache-v1')
              .then(cache => cache.put(event.request, clone));
            return response;
          })
          .catch(() => caches.match(event.request))
      );
    });
  `,

  // Stale While Revalidate
  staleWhileRevalidate: `
    self.addEventListener('fetch', event => {
      event.respondWith(
        caches.match(event.request)
          .then(cached => {
            const fetchPromise = fetch(event.request)
              .then(response => {
                caches.open('cache-v1')
                  .then(cache => cache.put(event.request, response.clone()));
                return response;
              });
            return cached || fetchPromise;
          })
      );
    });
  `,

  // Cache Only
  cacheOnly: `
    self.addEventListener('fetch', event => {
      event.respondWith(caches.match(event.request));
    });
  `,

  // Network Only
  networkOnly: `
    self.addEventListener('fetch', event => {
      event.respondWith(fetch(event.request));
    });
  `,
};

// 2. 完整Service Worker实现
const serviceWorkerCode = `
const CACHE_NAME = 'app-cache-v1';
const STATIC_ASSETS = [
  '/',
  '/index.html',
  '/styles/main.css',
  '/scripts/main.js',
  '/images/logo.png',
];

// 安装阶段：预缓存
self.addEventListener('install', event => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then(cache => cache.addAll(STATIC_ASSETS))
      .then(() => self.skipWaiting())
  );
});

// 激活阶段：清理旧缓存
self.addEventListener('activate', event => {
  event.waitUntil(
    caches.keys()
      .then(keys => Promise.all(
        keys
          .filter(key => key !== CACHE_NAME)
          .map(key => caches.delete(key))
      ))
      .then(() => self.clients.claim())
  );
});

// 请求阶段：智能缓存
self.addEventListener('fetch', event => {
  const { request } = event;
  const url = new URL(request.url);

  // API请求：网络优先
  if (url.pathname.startsWith('/api/')) {
    event.respondWith(networkFirst(request));
    return;
  }

  // 静态资源：缓存优先
  if (isStaticAsset(url.pathname)) {
    event.respondWith(cacheFirst(request));
    return;
  }

  // 页面导航：Stale While Revalidate
  if (request.mode === 'navigate') {
    event.respondWith(staleWhileRevalidate(request));
    return;
  }

  // 默认：网络优先
  event.respondWith(networkFirst(request));
});

// 缓存策略函数
async function cacheFirst(request) {
  const cached = await caches.match(request);
  if (cached) return cached;

  const response = await fetch(request);
  const cache = await caches.open(CACHE_NAME);
  cache.put(request, response.clone());
  return response;
}

async function networkFirst(request) {
  try {
    const response = await fetch(request);
    const cache = await caches.open(CACHE_NAME);
    cache.put(request, response.clone());
    return response;
  } catch (error) {
    const cached = await caches.match(request);
    if (cached) return cached;
    throw error;
  }
}

async function staleWhileRevalidate(request) {
  const cached = await caches.match(request);
  const fetchPromise = fetch(request)
    .then(response => {
      caches.open(CACHE_NAME)
        .then(cache => cache.put(request, response.clone()));
      return response;
    });

  return cached || fetchPromise;
}

function isStaticAsset(pathname) {
  return /\\.(js|css|png|jpg|jpeg|gif|svg|woff|woff2|ico)$/.test(pathname);
}
`;

// 3. Service Worker注册
function registerServiceWorker() {
  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('/sw.js')
      .then(registration => {
        console.log('Service Worker注册成功:', registration.scope);

        // 检查更新
        registration.addEventListener('updatefound', () => {
          const newWorker = registration.installing;
          newWorker.addEventListener('statechange', () => {
            if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {
              // 有新版本可用
              showUpdateNotification();
            }
          });
        });
      })
      .catch(error => {
        console.error('Service Worker注册失败:', error);
      });
  }
}
```

### 4.3 缓存最佳实践

```javascript
// 缓存最佳实践

// 1. 版本化文件名
const versioningStrategy = {
  // 内容哈希
  contentHash: `
    // webpack配置
    output: {
      filename: '[name].[contenthash].js',
    }
  `,

  // 版本号
  version: `
    /css/main.v1.0.0.css
    /js/app.v1.0.0.js
  `,

  // 组合使用
  combined: `
    /css/main.a1b2c3d4.css
    /js/app.e5f6g7h8.js
  `,
};

// 2. 缓存清除策略
const cacheEvictionStrategy = {
  // LRU（最近最少使用）
  lru: '删除最长时间未使用的缓存',

  // LFU（最近最少使用频率）
  lfu: '删除访问频率最低的缓存',

  // FIFO（先进先出）
  fifo: '删除最早添加的缓存',

  // 容量限制
  capacity: '当缓存数量达到上限时删除旧缓存',
};

// 3. 调试缓存
function debugCache() {
  // 列出所有缓存
  caches.keys().then(names => {
    console.log('所有缓存:', names);
    names.forEach(name => {
      caches.open(name).then(cache => {
        cache.keys().then(keys => {
          console.log(`缓存 ${name}:`, keys.length, '项');
          keys.forEach(request => {
            console.log(' -', request.url);
          });
        });
      });
    });
  });

  // 删除特定缓存
  caches.delete('old-cache-name');

  // 清除所有缓存
  caches.keys().then(names => Promise.all(names.map(caches.delete)));
}
```

---

## 5. CDN与边缘计算

### 5.1 CDN配置优化

```javascript
// CDN优化配置

// 1. 边缘缓存配置
const cdnCacheConfig = {
  // 静态资源
  static: {
    '**/*.js': { maxAge: '1y', staleWhileRevalidate: '7d' },
    '**/*.css': { maxAge: '1y', staleWhileRevalidate: '7d' },
    '**/*.{png,jpg,jpeg,gif,svg,ico}': { maxAge: '1y' },
    '**/*.woff2': { maxAge: '1y' },
  },

  // HTML页面
  html: {
    '**/*.html': { maxAge: '0', noCache: true },
  },

  // API响应
  api: {
    '/api/data': { maxAge: '1h' },
    '/api/config': { maxAge: '24h' },
    '/api/**': { maxAge: '0', noStore: true },
  },
};

// 2. 压缩配置
const compressionConfig = {
  // 启用Brotli（比Gzip更好）
  brotli: {
    enabled: true,
    level: 6, // 1-11
    types: ['text/html', 'text/css', 'text/javascript', 'application/json'],
  },

  // Gzip备用
  gzip: {
    enabled: true,
    level: 6,
    types: ['text/html', 'text/css', 'text/javascript', 'application/json'],
  },
};

// 3. 图片优化配置
const imageOptimizationConfig = {
  // WebP自动转换
  webp: {
    enabled: true,
    quality: 85,
  },

  // AVIF（更好的压缩）
  avif: {
    enabled: true,
    quality: 80,
    // 浏览器不支持时回退到WebP
    fallback: 'webp',
  },

  // 响应式图片
  responsive: {
    widths: [320, 640, 960, 1280, 1920],
    formats: ['avif', 'webp', 'original'],
  },

  // 懒加载
  lazyload: {
    enabled: true,
    threshold: '200px',
  },
};
```

### 5.2 边缘计算应用

```javascript
// 边缘计算优化

// 1. 边缘重定向
const edgeRedirect = `
addEventListener('fetch', event => {
  const url = new URL(event.request.url);

  // 移动端重定向
  if (url.pathname.startsWith('/desktop/')) {
    const mobileUrl = url.href.replace('/desktop/', '/mobile/');
    return Response.redirect(mobileUrl, 301);
  }

  // 地理重定向
  const country = event.request.cf.country;
  if (country === 'CN' && !url.pathname.startsWith('/cn/')) {
    return Response.redirect('/cn' + url.pathname, 302);
  }
});
`;

// 2. 边缘A/B测试
const edgeABTest = `
addEventListener('fetch', event => {
  const url = new URL(event.request.url);

  // 基于Cookie或随机分配
  let variant = getCookie('ab-variant') || (Math.random() > 0.5 ? 'A' : 'B');

  // 为新用户设置Cookie
  if (!getCookie('ab-variant')) {
    const response = fetch(event.request);
    const newResponse = new Response(response.body, response);
    newResponse.headers.set('Set-Cookie', 'ab-variant=' + variant + '; Path=/');
    return newResponse;
  }

  // 返回对应版本
  if (variant === 'A') {
    return fetch('/version-a' + url.pathname);
  } else {
    return fetch('/version-b' + url.pathname);
  }
});
`;

// 3. 边缘个性化
const edgePersonalization = `
addEventListener('fetch', event => {
  const request = event.request;
  const country = request.cf.country;
  const city = request.cf.city;
  const device = request.cf.deviceType;

  // 基于地理位置的内容
  let contentUrl = '/content/global' + url.pathname;
  if (country === 'US') contentUrl = '/content/us' + url.pathname;
  if (country === 'JP') contentUrl = '/content/jp' + url.pathname;

  // 添加响应头
  const response = fetch(contentUrl);
  response.headers.set('X-Country', country);
  response.headers.set('X-City', city);
  response.headers.set('X-Device', device);

  return response;
});
`;

// 4. 边缘安全
const edgeSecurity = `
addEventListener('fetch', event => {
  const request = event.request;

  // 检查恶意请求
  if (isMalicious(request)) {
    return new Response('Blocked', { status: 403 });
  }

  // 添加安全头
  const response = fetch(request);
  response.headers.set('X-Frame-Options', 'DENY');
  response.headers.set('X-Content-Type-Options', 'nosniff');
  response.headers.set('Referrer-Policy', 'strict-origin-when-cross-origin');

  return response;
});
`;
```

---

## 6. API性能优化

### 6.1 请求优化

```javascript
// API请求优化

// 1. 请求合并
async function batchAPIRequests() {
  // 不好的做法：多个请求
  const user = await fetch('/api/user').then(r => r.json());
  const posts = await fetch('/api/posts').then(r => r.json());
  const config = await fetch('/api/config').then(r => r.json());

  // 好的做法：批量请求
  const response = await fetch('/api/batch?queries=user,posts,config');
  const data = await response.json();

  return {
    user: data.user,
    posts: data.posts,
    config: data.config,
  };
}

// 2. 请求去重
class RequestDeduplicator {
  constructor() {
    this.pending = new Map();
  }

  async fetch(url, options = {}) {
    const key = url + JSON.stringify(options);

    if (this.pending.has(key)) {
      return this.pending.get(key);
    }

    const promise = fetch(url, options)
      .then(r => {
        if (!r.ok) throw new Error(`HTTP ${r.status}`);
        return r.json();
      })
      .finally(() => this.pending.delete(key));

    this.pending.set(key, promise);
    return promise;
  }
}

// 3. 请求取消
function cancellableFetch() {
  const controller = new AbortController();

  // 5秒超时
  setTimeout(() => controller.abort(), 5000);

  return fetch('/api/data', { signal: controller.signal })
    .then(r => r.json())
    .catch(error => {
      if (error.name === 'AbortError') {
        console.log('请求超时');
      }
      throw error;
    });
}

// 4. 重试机制
async function retryFetch(url, options = {}) {
  const { maxRetries = 3, retryDelay = 1000 } = options;

  for (let i = 0; i < maxRetries; i++) {
    try {
      const response = await fetch(url);
      if (response.ok) return response.json();
      throw new Error(`HTTP ${response.status}`);
    } catch (error) {
      if (i === maxRetries - 1) throw error;
      await sleep(retryDelay * Math.pow(2, i)); // 指数退避
    }
  }
}
```

### 6.2 响应优化

```javascript
// API响应优化

// 1. 字段过滤
async function fetchWithFields() {
  // 只请求需要的字段
  const response = await fetch('/api/users?fields=id,name,email&expand=posts');
  const user = await response.json();

  return user;
}

// 2. 分页优化
async function paginatedFetch() {
  const page = 1;
  const pageSize = 20;

  const response = await fetch(
    `/api/items?page=${page}&limit=${pageSize}&sort=-createdAt`
  );

  const { data, meta } = await response.json();

  return {
    items: data,
    pagination: {
      page: meta.page,
      pageSize: meta.pageSize,
      total: meta.total,
      totalPages: Math.ceil(meta.total / meta.pageSize),
    },
  };
}

// 3. 游标分页（适合大数据集）
async function cursorPagination() {
  let cursor = null;
  const items = [];

  do {
    const url = cursor
      ? `/api/items?cursor=${cursor}&limit=100`
      : '/api/items?limit=100';

    const response = await fetch(url);
    const { data, nextCursor } = await response.json();

    items.push(...data);
    cursor = nextCursor;
  } while (cursor);

  return items;
}

// 4. 压缩响应
async function compressedFetch() {
  const response = await fetch('/api/large-data', {
    headers: {
      'Accept-Encoding': 'gzip, deflate, br',
    },
  });

  // 浏览器自动解压
  return response.json();
}
```

### 6.3 GraphQL优化

```javascript
// GraphQL优化

// 1. 查询优化
const graphqlQuery = `
  # 只请求需要的字段
  query {
    user(id: 1) {
      id
      name
      email
      posts(first: 10) {
        edges {
          node {
            id
            title
            # 不需要的内容不要请求
          }
        }
      }
    }
  }
`;

// 2. 批量查询
const batchQuery = `
  # 一次性请求多个资源
  query {
    currentUser {
      id
      name
    }
    siteConfig {
      title
    }
    notifications {
      count
    }
  }
`;

// 3. 缓存策略
const graphqlCache = {
  // 持久化查询
  persistedQueries: `
    mutation SaveQuery($id: ID!, $hash: String!, $query: String!) {
      savePersistedQuery(id: $id, hash: $hash, query: $query) {
        success
      }
    }
  `,

  // 缓存控制
  cacheControl: `
    query {
      user @cacheControl(maxAge: 300) {
        id
        name
      }
      posts @cacheControl(maxAge: 60) {
        id
        title
      }
    }
  `,
};

// 4. DataLoader批处理
const dataLoaderExample = `
  // 避免N+1问题
  class PostLoader extends DataLoader {
    constructor() {
      super(keys => batchLoadPosts(keys));
    }
  }

  // 使用
  const postLoader = new PostLoader();
  const posts = userIds.map(id => postLoader.load(id));
`;
```

---

## 7. 移动端网络优化

### 7.1 移动网络特性

```javascript
// 移动网络优化

// 1. 网络类型检测
function detectNetworkType() {
  const connection = navigator.connection ||
    navigator.mozConnection ||
    navigator.webkitConnection;

  if (!connection) return 'unknown';

  return {
    effectiveType: connection.effectiveType, // '4g', '3g', '2g', 'slow-2g'
    downlink: connection.downlink, // 带宽估计 (Mbps)
    rtt: connection.rtt, // 往返时间 (ms)
    saveData: connection.saveData, // 用户节省数据模式
  };
}

// 2. 自适应质量
function adaptiveQuality() {
  const connection = navigator.connection;

  if (!connection) return;

  // 根据网络类型调整质量
  switch (connection.effectiveType) {
    case '4g':
      // 高质量
      loadHighQualityImages();
      enableDetailedMaps();
      break;

    case '3g':
      // 中等质量
      loadMediumQualityImages();
      break;

    case '2g':
    case 'slow-2g':
      // 低质量
      loadLowQualityImages();
      disableMaps();
      break;
  }

  // 监听网络变化
  connection.addEventListener('change', () => {
    adaptiveQuality();
  });
}

// 3. 节省数据模式
function respectSaveData() {
  const connection = navigator.connection;

  if (connection?.saveData) {
    // 用户启用了节省数据模式
    skipAnalytics();
    loadLowQualityImages();
    skipVideoAutoplay();
    loadTextOnlyContent();
  }
}
```

### 7.2 移动端优化策略

```javascript
// 移动端网络优化策略

const mobileOptimizations = {
  // 1. 减少请求数量
  reduceRequests: {
    合并资源: '将多个CSS/JS合并成单个文件',
    内联关键资源: '将关键CSS内联到HTML',
    延迟加载: '非关键资源延迟加载',
  },

  // 2. 减少数据传输量
  reduceData: {
    压缩: '启用Gzip/Brotli压缩',
    精简: '移除不必要的代码和注释',
    现代格式: '使用WebP等高效格式',
    响应式资源: '根据设备提供适当大小的资源',
  },

  // 3. 优化连接
  optimizeConnection: {
    预连接: 'preconnect关键域名',
    并行限制: '限制同时连接数',
    KeepAlive: '使用HTTP Keep-Alive',
  },

  // 4. 离线支持
  offlineSupport: {
    ServiceWorker: '缓存关键资源',
    本地存储: '缓存用户数据',
    离线指示: '显示离线状态',
  },
};

// 性能预算
const mobileBudget = {
  // 首屏资源预算（3G网络）
  firstPaint: {
    html: '14KB',
    criticalCSS: '10KB',
    criticalJS: '50KB',
    images: '100KB',
    total: '174KB',
  },

  // 总资源预算
  total: {
    js: '200KB',
    css: '50KB',
    images: '200KB',
    fonts: '50KB',
    total: '500KB',
  },
};
```

---

## 8. 面试高频问题

### 问题1：如何优化首屏加载性能？

**答案：**

1. **减少关键资源**
   - 关键CSS内联
   - 延迟加载非关键CSS/JS
   - 压缩HTML

2. **优化资源加载**
   - 预加载关键资源
   - 预连接关键域名
   - 使用prefetch预取

3. **优化服务器**
   - 启用HTTP/2
   - 使用CDN
   - 启用缓存
   - 压缩响应

4. **优化图片**
   - 使用现代格式（WebP/AVIF）
   - 响应式图片
   - 懒加载

### 问题2：强缓存和协商缓存的区别？

**答案：**

| 特性 | 强缓存 | 协商缓存 |
|------|--------|----------|
| 是否发送请求 | 不发送 | 发送 |
| 响应码 | 200（from cache） | 304 |
| 控制头 | Cache-Control | ETag/Last-Modified |
| 刷新行为 | 不重新请求 | 重新验证 |

### 问题3：HTTP/2有哪些优化？

**答案：**

1. **多路复用** - 单TCP连接并行传输
2. **头部压缩** - HPACK算法
3. **服务器推送** - 主动推送资源
4. **流优先级** - 资源优先级
5. **二进制传输** - 更高效的解析

### 问题4：如何实现离线支持？

**答案：**

1. **Service Worker**
   - 缓存关键资源
   - 实现离线Fallback
   - 后台同步

2. **Cache API**
   - 缓存HTML/CSS/JS
   - 缓存图片
   - 缓存API响应

3. **本地存储**
   - IndexedDB存储数据
   - localStorage存储配置

4. **离线检测**
   - navigator.onLine
   - online/offline事件

### 问题5：移动端网络优化有哪些策略？

**答案：**

1. **网络检测**
   - 检测网络类型
   - 监听网络变化
   - 尊重节省数据模式

2. **自适应质量**
   - 根据网络加载不同质量资源
   - 动态调整

3. **减少请求**
   - 合并资源
   - 减少数据量
   - 延迟加载

4. **离线缓存**
   - Service Worker缓存
   - 本地数据缓存

---

## 最佳实践总结

### 网络性能优化清单

- [ ] 启用HTTP/2或HTTP/3
- [ ] 配置强缓存和协商缓存
- [ ] 使用CDN加速
- [ ] 预加载关键资源
- [ ] 预连接关键域名
- [ ] 压缩所有资源
- [ ] 使用现代图片格式
- [ ] 实现Service Worker
- [ ] 监控网络性能
- [ ] 优化移动端体验

---

*本文档最后更新于 2026年3月*
