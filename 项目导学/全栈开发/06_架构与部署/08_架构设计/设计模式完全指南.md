# 设计模式完全指南

## 目录

1. [设计模式基础](#1-设计模式基础)
2. [创建型模式](#2-创建型模式)
3. [结构型模式](#3-结构型模式)
4. [行为型模式](#4-行为型模式)
5. [JavaScript设计模式](#5-javascript设计模式)
6. [React设计模式](#6-react设计模式)

---

## 1. 设计模式基础

### 1.1 设计模式分类

```
设计模式三大类

创建型模式（Creational Patterns）
├── 单例模式（Singleton）
├── 工厂模式（Factory）
├── 抽象工厂模式（Abstract Factory）
├── 建造者模式（Builder）
└── 原型模式（Prototype）

结构型模式（Structural Patterns）
├── 适配器模式（Adapter）
├── 装饰器模式（Decorator）
├── 代理模式（Proxy）
├── 外观模式（Facade）
├── 桥接模式（Bridge）
├── 组合模式（Composite）
└── 享元模式（Flyweight）

行为型模式（Behavioral Patterns）
├── 策略模式（Strategy）
├── 观察者模式（Observer）
├── 命令模式（Command）
├── 迭代器模式（Iterator）
├── 模板方法模式（Template Method）
├── 责任链模式（Chain of Responsibility）
├── 状态模式（State）
├── 备忘录模式（Memento）
└── 访问者模式（Visitor）
```

### 1.2 设计原则（SOLID）

```
SOLID原则

S - 单一职责原则（Single Responsibility Principle）
    └── 一个类只负责一件事

O - 开闭原则（Open-Closed Principle）
    └── 对扩展开放，对修改关闭

L - 里氏替换原则（Liskov Substitution Principle）
    └── 子类可以替换父类

I - 接口隔离原则（Interface Segregation Principle）
    └── 接口应该小而专一

D - 依赖倒置原则（Dependency Inversion Principle）
    └── 依赖抽象，不依赖具体实现
```

---

## 2. 创建型模式

### 2.1 单例模式

```javascript
// 1. 基础单例
class Singleton {
    constructor() {
        if (Singleton.instance) {
            return Singleton.instance;
        }
        Singleton.instance = this;
    }

    static getInstance() {
        if (!Singleton.instance) {
            Singleton.instance = new Singleton();
        }
        return Singleton.instance;
    }
}

// 使用
const instance1 = new Singleton();
const instance2 = Singleton.getInstance();
console.log(instance1 === instance2);  // true

// 2. 模块单例（ES6模块）
// database.js
class Database {
    constructor() {
        this.connection = null;
    }

    connect() {
        if (!this.connection) {
            this.connection = createConnection();
        }
        return this.connection;
    }
}

export default new Database();  // 模块导出单例

// 3. 对象字面量单例
const Config = {
    apiUrl: 'https://api.example.com',
    timeout: 5000,

    getApiUrl() {
        return this.apiUrl;
    },

    setTimeout(value) {
        this.timeout = value;
    },
};

// 4. 闭包单例
const Singleton = (function () {
    let instance;

    function createInstance() {
        return {
            name: 'Singleton',
            method() {
                console.log('Singleton method');
            },
        };
    }

    return {
        getInstance() {
            if (!instance) {
                instance = createInstance();
            }
            return instance;
        },
    };
})();

// 5. TypeScript单例
class Singleton {
    private static instance: Singleton;

    private constructor() {
        // 私有构造函数
    }

    public static getInstance(): Singleton {
        if (!Singleton.instance) {
            Singleton.instance = new Singleton();
        }
        return Singleton.instance;
    }

    public businessLogic(): void {
        console.log('Singleton business logic');
    }
}

// 使用
const singleton = Singleton.getInstance();
singleton.businessLogic();
```

### 2.2 工厂模式

```javascript
// 1. 简单工厂
class UserFactory {
    static create(type) {
        switch (type) {
            case 'admin':
                return new AdminUser();
            case 'member':
                return new MemberUser();
            case 'guest':
                return new GuestUser();
            default:
                throw new Error('未知用户类型');
        }
    }
}

class AdminUser {
    constructor() {
        this.role = 'admin';
        this.permissions = ['read', 'write', 'delete'];
    }
}

class MemberUser {
    constructor() {
        this.role = 'member';
        this.permissions = ['read', 'write'];
    }
}

class GuestUser {
    constructor() {
        this.role = 'guest';
        this.permissions = ['read'];
    }
}

// 使用
const admin = UserFactory.create('admin');

// 2. 工厂方法模式
class VehicleFactory {
    createVehicle() {
        throw new Error('必须实现createVehicle方法');
    }
}

class CarFactory extends VehicleFactory {
    createVehicle() {
        return new Car();
    }
}

class BikeFactory extends VehicleFactory {
    createVehicle() {
        return new Bike();
    }
}

class Car {
    drive() {
        console.log('驾驶汽车');
    }
}

class Bike {
    ride() {
        console.log('骑行自行车');
    }
}

// 使用
const carFactory = new CarFactory();
const car = carFactory.createVehicle();
car.drive();

// 3. 抽象工厂模式
class UIFactory {
    createButton() {
        throw new Error('必须实现createButton方法');
    }

    createInput() {
        throw new Error('必须实现createInput方法');
    }
}

class WindowsFactory extends UIFactory {
    createButton() {
        return new WindowsButton();
    }

    createInput() {
        return new WindowsInput();
    }
}

class MacOSFactory extends UIFactory {
    createButton() {
        return new MacOSButton();
    }

    createInput() {
        return new MacOSInput();
    }
}

class WindowsButton {
    render() {
        console.log('Windows按钮');
    }
}

class MacOSButton {
    render() {
        console.log('macOS按钮');
    }
}

// 使用
const factory = new MacOSFactory();
const button = factory.createButton();
button.render();

// 4. 配置驱动的工厂
const products = {
    productA: {
        create: () => new ProductA(),
    },
    productB: {
        create: () => new ProductB(),
    },
};

class ProductFactory {
    static create(type, config) {
        const product = products[type];

        if (!product) {
            throw new Error(`产品类型 ${type} 不存在`);
        }

        return product.create(config);
    }
}

// 5. 动态工厂
class DynamicFactory {
    constructor() {
        this.registry = new Map();
    }

    register(name, creator) {
        this.registry.set(name, creator);
    }

    create(name, ...args) {
        const creator = this.registry.get(name);

        if (!creator) {
            throw new Error(`未注册的创建器: ${name}`);
        }

        return creator(...args);
    }
}

// 使用
const factory = new DynamicFactory();

factory.register('user', (name, email) => ({
    name,
    email,
    createdAt: new Date(),
}));

const user = factory.create('user', '张三', 'zhangsan@example.com');
```

### 2.3 建造者模式

```javascript
// 1. 基础建造者
class QueryBuilder {
    constructor() {
        this.query = {
            select: [],
            from: '',
            where: [],
            orderBy: [],
            limit: null,
        };
    }

    select(...fields) {
        this.query.select = fields;
        return this;
    }

    from(table) {
        this.query.from = table;
        return this;
    }

    where(condition) {
        this.query.where.push(condition);
        return this;
    }

    orderBy(field, direction = 'ASC') {
        this.query.orderBy.push({ field, direction });
        return this;
    }

    limit(count) {
        this.query.limit = count;
        return this;
    }

    build() {
        let sql = `SELECT ${this.query.select.join(', ')} FROM ${this.query.from}`;

        if (this.query.where.length > 0) {
            sql += ` WHERE ${this.query.where.join(' AND ')}`;
        }

        if (this.query.orderBy.length > 0) {
            const order = this.query.orderBy
                .map(o => `${o.field} ${o.direction}`)
                .join(', ');
            sql += ` ORDER BY ${order}`;
        }

        if (this.query.limit) {
            sql += ` LIMIT ${this.query.limit}`;
        }

        return sql;
    }
}

// 使用
const query = new QueryBuilder()
    .select('id', 'name', 'email')
    .from('users')
    .where('status = "active"')
    .where('age > 18')
    .orderBy('created_at', 'DESC')
    .limit(10)
    .build();

console.log(query);
// SELECT id, name, email FROM users WHERE status = "active" AND age > 18 ORDER BY created_at DESC LIMIT 10

// 2. 对象建造者
class HttpRequestBuilder {
    constructor() {
        this.config = {
            method: 'GET',
            url: '',
            headers: {},
            params: {},
            data: null,
            timeout: 5000,
        };
    }

    setMethod(method) {
        this.config.method = method;
        return this;
    }

    setUrl(url) {
        this.config.url = url;
        return this;
    }

    setHeaders(headers) {
        this.config.headers = { ...this.config.headers, ...headers };
        return this;
    }

    setParams(params) {
        this.config.params = { ...this.config.params, ...params };
        return this;
    }

    setData(data) {
        this.config.data = data;
        return this;
    }

    setTimeout(timeout) {
        this.config.timeout = timeout;
        return this;
    }

    build() {
        return this.config;
    }
}

// 使用
const request = new HttpRequestBuilder()
    .setMethod('POST')
    .setUrl('https://api.example.com/users')
    .setHeaders({ 'Content-Type': 'application/json' })
    .setData({ name: '张三' })
    .setTimeout(10000)
    .build();

// 3. 复杂对象建造者
class UserBuilder {
    constructor() {
        this.user = {
            name: '',
            email: '',
            age: 0,
            address: {},
            phone: '',
            preferences: {},
        };
    }

    setName(name) {
        this.user.name = name;
        return this;
    }

    setEmail(email) {
        this.user.email = email;
        return this;
    }

    setAge(age) {
        this.user.age = age;
        return this;
    }

    setAddress(street, city, zipCode) {
        this.user.address = { street, city, zipCode };
        return this;
    }

    setPhone(phone) {
        this.user.phone = phone;
        return this;
    }

    setPreferences(theme, language) {
        this.user.preferences = { theme, language };
        return this;
    }

    build() {
        // 验证必填字段
        if (!this.user.name || !this.user.email) {
            throw new Error('姓名和邮箱为必填项');
        }

        return this.user;
    }
}

// 使用
const user = new UserBuilder()
    .setName('张三')
    .setEmail('zhangsan@example.com')
    .setAge(25)
    .setAddress('建国路88号', '北京', '100022')
    .setPhone('13800138000')
    .setPreferences('dark', 'zh-CN')
    .build();
```

---

## 3. 结构型模式

### 3.1 适配器模式

```javascript
// 1. 类适配器
// 旧接口
class OldPrinter {
    print(text) {
        console.log(`旧打印机: ${text}`);
    }
}

// 新接口
class NewPrinter {
    printDocument(document) {
        console.log(`新打印机: ${document.content}`);
    }
}

// 适配器
class PrinterAdapter extends OldPrinter {
    constructor(newPrinter) {
        super();
        this.newPrinter = newPrinter;
    }

    print(text) {
        // 将旧接口调用转换为新接口
        const document = { content: text };
        this.newPrinter.printDocument(document);
    }
}

// 使用
const newPrinter = new NewPrinter();
const adapter = new PrinterAdapter(newPrinter);
adapter.print('Hello World');

// 2. 对象适配器
// 第三方API
const thirdPartyAPI = {
    getUserName: (id) => `User ${id}`,
    getUserAge: (id) => 25,
};

// 期望的接口
class UserAPI {
    getUser(id) {
        throw new Error('必须实现getUser方法');
    }
}

// 适配器
class UserAPIAdapter extends UserAPI {
    constructor(thirdPartyAPI) {
        super();
        this.api = thirdPartyAPI;
    }

    getUser(id) {
        return {
            id,
            name: this.api.getUserName(id),
            age: this.api.getUserAge(id),
        };
    }
}

// 使用
const userAPI = new UserAPIAdapter(thirdPartyAPI);
const user = userAPI.getUser(1);

// 3. 数据适配器
class DataAdapter {
    // 将XML转换为JSON
    static xmlToJson(xml) {
        // 解析XML并转换为JSON
        return JSON.parse(JSON.stringify(xml));
    }

    // 将CSV转换为JSON
    static csvToJson(csv) {
        const lines = csv.split('\n');
        const headers = lines[0].split(',');
        const data = [];

        for (let i = 1; i < lines.length; i++) {
            const values = lines[i].split(',');
            const row = {};

            headers.forEach((header, index) => {
                row[header] = values[index];
            });

            data.push(row);
        }

        return data;
    }

    // 将JSON转换为CSV
    static jsonToCsv(json) {
        if (!Array.isArray(json) || json.length === 0) {
            return '';
        }

        const headers = Object.keys(json[0]);
        const csvRows = [headers.join(',')];

        json.forEach((row) => {
            const values = headers.map((header) => row[header]);
            csvRows.push(values.join(','));
        });

        return csvRows.join('\n');
    }
}

// 4. 接口适配器
class PaymentAdapter {
    constructor() {
        this.processors = new Map();
    }

    registerProcessor(name, processor) {
        this.processors.set(name, processor);
    }

    async pay(processorName, amount, currency) {
        const processor = this.processors.get(processorName);

        if (!processor) {
            throw new Error(`支付处理器 ${processorName} 不存在`);
        }

        // 统一支付接口
        const result = await processor.processPayment({
            amount,
            currency,
        });

        // 标准化返回结果
        return {
            success: result.status === 'success',
            transactionId: result.id,
            amount,
            currency,
        };
    }
}

// 使用
const paymentAdapter = new PaymentAdapter();

paymentAdapter.registerProcessor('alipay', {
    processPayment: async (data) => {
        // 支付宝处理逻辑
        return { status: 'success', id: 'ALI123' };
    },
});

paymentAdapter.registerProcessor('wechat', {
    processPayment: async (data) => {
        // 微信支付处理逻辑
        return { status: 'success', id: 'WX456' };
    },
});

const result = await paymentAdapter.pay('alipay', 100, 'CNY');
```

### 3.2 装饰器模式

```javascript
// 1. 函数装饰器
// 日志装饰器
function withLogging(target, key, descriptor) {
    const originalMethod = descriptor.value;

    descriptor.value = function (...args) {
        console.log(`调用 ${key}，参数:`, args);
        const result = originalMethod.apply(this, args);
        console.log(`${key} 返回:`, result);
        return result;
    };

    return descriptor;
}

class Calculator {
    @withLogging
    add(a, b) {
        return a + b;
    }
}

// 2. 类装饰器
function sealed(constructor) {
    Object.seal(constructor);
    Object.seal(constructor.prototype);
}

@sealed
class BankAccount {
    constructor(balance) {
        this.balance = balance;
    }
}

// 3. 装饰器组合
function readonly(target, key, descriptor) {
    descriptor.writable = false;
    return descriptor;
}

function enumerable(target, key, descriptor) {
    descriptor.enumerable = true;
    return descriptor;
}

class Person {
    @readonly
    @enumerable
    name = '张三';
}

// 4. 动态装饰器
class Coffee {
    cost() {
        return 5;
    }

    description() {
        return '普通咖啡';
    }
}

class MilkDecorator {
    constructor(coffee) {
        this.coffee = coffee;
    }

    cost() {
        return this.coffee.cost() + 2;
    }

    description() {
        return `${this.coffee.description()}，加牛奶`;
    }
}

class SugarDecorator {
    constructor(coffee) {
        this.coffee = coffee;
    }

    cost() {
        return this.coffee.cost() + 1;
    }

    description() {
        return `${this.coffee.description()}，加糖`;
    }
}

// 使用
let coffee = new Coffee();
coffee = new MilkDecorator(coffee);
coffee = new SugarDecorator(coffee);

console.log(coffee.description());  // 普通咖啡，加牛奶，加糖
console.log(coffee.cost());  // 8

// 5. React高阶组件（HOC）
function withLoading(WrappedComponent) {
    return function WithLoadingComponent({ isLoading, ...props }) {
        if (isLoading) {
            return <div>加载中...</div>;
        }
        return <WrappedComponent {...props} />;
    };
}

// 使用
const UserListWithLoading = withLoading(UserList);

<UserListWithLoading isLoading={loading} users={users} />

// 6. 函数装饰器工具
function debounce(func, delay) {
    let timeoutId;

    return function (...args) {
        clearTimeout(timeoutId);
        timeoutId = setTimeout(() => func.apply(this, args), delay);
    };
}

function throttle(func, limit) {
    let inThrottle;

    return function (...args) {
        if (!inThrottle) {
            func.apply(this, args);
            inThrottle = true;
            setTimeout(() => (inThrottle = false), limit);
        }
    };
}

// 使用
const handleScroll = debounce(() => {
    console.log('滚动事件');
}, 200);

const handleResize = throttle(() => {
    console.log('调整大小事件');
}, 200);
```

### 3.3 代理模式

```javascript
// 1. 虚拟代理（延迟加载）
class ImageProxy {
    constructor(url) {
        this.url = url;
        this.image = null;
    }

    display() {
        if (!this.image) {
            console.log('加载图片...');
            this.image = new Image();
            this.image.src = this.url;
        }
        console.log('显示图片:', this.url);
    }
}

// 2. 保护代理（访问控制）
class UserProxy {
    constructor(user, currentUser) {
        this.user = user;
        this.currentUser = currentUser;
    }

    getProfile() {
        if (this.currentUser.role === 'admin') {
            return this.user.profile;
        }
        return { name: this.user.name };
    }

    updateEmail(newEmail) {
        if (this.currentUser.id !== this.user.id) {
            throw new Error('无权修改');
        }
        this.user.email = newEmail;
    }
}

// 3. 缓存代理
class DataServiceProxy {
    constructor(service) {
        this.service = service;
        this.cache = new Map();
    }

    async getData(key) {
        if (this.cache.has(key)) {
            console.log('从缓存获取:', key);
            return this.cache.get(key);
        }

        console.log('从服务获取:', key);
        const data = await this.service.getData(key);
        this.cache.set(key, data);

        return data;
    }
}

// 4. ES6 Proxy
const user = {
    name: '张三',
    age: 25,
};

const proxy = new Proxy(user, {
    get(target, property) {
        console.log(`获取属性: ${property}`);
        return target[property];
    },

    set(target, property, value) {
        console.log(`设置属性: ${property} = ${value}`);
        target[property] = value;
        return true;
    },

    has(target, property) {
        console.log(`检查属性: ${property}`);
        return property in target;
    },

    deleteProperty(target, property) {
        console.log(`删除属性: ${property}`);
        delete target[property];
        return true;
    },
});

// 5. 验证代理
function createValidator(target, validators) {
    return new Proxy(target, {
        set(obj, prop, value) {
            if (validators[prop]) {
                const validator = validators[prop];

                if (!validator(value)) {
                    throw new Error(`无效的值: ${prop}`);
                }
            }

            obj[prop] = value;
            return true;
        },
    });
}

const person = createValidator(
    {},
    {
        age: (value) => Number.isInteger(value) && value > 0,
        email: (value) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value),
    }
);

person.age = 25;  // OK
person.email = 'user@example.com';  // OK
person.age = -5;  // Error
```

---

## 4. 行为型模式

### 4.1 观察者模式

```javascript
// 1. 基础观察者
class EventEmitter {
    constructor() {
        this.events = {};
    }

    on(event, callback) {
        if (!this.events[event]) {
            this.events[event] = [];
        }
        this.events[event].push(callback);
    }

    emit(event, ...args) {
        if (this.events[event]) {
            this.events[event].forEach((callback) => callback(...args));
        }
    }

    off(event, callback) {
        if (this.events[event]) {
            this.events[event] = this.events[event].filter((cb) => cb !== callback);
        }
    }

    once(event, callback) {
        const onceCallback = (...args) => {
            callback(...args);
            this.off(event, onceCallback);
        };
        this.on(event, onceCallback);
    }
}

// 使用
const emitter = new EventEmitter();

emitter.on('message', (msg) => {
    console.log('收到消息:', msg);
});

emitter.emit('message', 'Hello');

// 2. 发布-订阅模式
class PubSub {
    constructor() {
        this.subscribers = {};
    }

    subscribe(event, callback) {
        if (!this.subscribers[event]) {
            this.subscribers[event] = [];
        }
        this.subscribers[event].push(callback);

        return {
            unsubscribe: () => {
                this.subscribers[event] = this.subscribers[event].filter(
                    (cb) => cb !== callback
                );
            },
        };
    }

    publish(event, data) {
        if (this.subscribers[event]) {
            this.subscribers[event].forEach((callback) => callback(data));
        }
    }
}

// 使用
const pubsub = new PubSub();

const subscription = pubsub.subscribe('news', (data) => {
    console.log('新闻:', data);
});

pubsub.publish('news', '重大新闻');

subscription.unsubscribe();

// 3. RxJS风格的观察者
class Observable {
    constructor(subscribe) {
        this.subscribe = subscribe;
    }

    static fromEvent(element, event) {
        return new Observable((observer) => {
            const handler = (e) => observer.next(e);
            element.addEventListener(event, handler);

            return {
                unsubscribe: () => {
                    element.removeEventListener(event, handler);
                },
            };
        });
    }

    map(fn) {
        return new Observable((observer) => {
            const subscription = this.subscribe({
                next: (value) => observer.next(fn(value)),
                error: (err) => observer.error(err),
                complete: () => observer.complete(),
            });

            return subscription;
        });
    }

    filter(fn) {
        return new Observable((observer) => {
            const subscription = this.subscribe({
                next: (value) => {
                    if (fn(value)) {
                        observer.next(value);
                    }
                },
                error: (err) => observer.error(err),
                complete: () => observer.complete(),
            });

            return subscription;
        });
    }
}

// 使用
const clicks$ = Observable.fromEvent(document, 'click');

const subscription = clicks$
    .map((e) => ({ x: e.clientX, y: e.clientY }))
    .filter((pos) => pos.x > 100)
    .subscribe({
        next: (pos) => console.log('点击位置:', pos),
        error: (err) => console.error('错误:', err),
        complete: () => console.log('完成'),
    });

// 4. 状态管理（Flux/Redux风格）
class Store {
    constructor(reducer, initialState) {
        this.reducer = reducer;
        this.state = initialState;
        this.listeners = [];
    }

    getState() {
        return this.state;
    }

    dispatch(action) {
        this.state = this.reducer(this.state, action);
        this.listeners.forEach((listener) => listener());
    }

    subscribe(listener) {
        this.listeners.push(listener);

        return () => {
            this.listeners = this.listeners.filter((l) => l !== listener);
        };
    }
}

// 使用
const reducer = (state = { count: 0 }, action) => {
    switch (action.type) {
        case 'INCREMENT':
            return { count: state.count + 1 };
        case 'DECREMENT':
            return { count: state.count - 1 };
        default:
            return state;
    }
};

const store = new Store(reducer);

store.subscribe(() => {
    console.log('状态更新:', store.getState());
});

store.dispatch({ type: 'INCREMENT' });
```

### 4.2 策略模式

```javascript
// 1. 基础策略
class PaymentStrategy {
    pay(amount) {
        throw new Error('必须实现pay方法');
    }
}

class AlipayStrategy extends PaymentStrategy {
    pay(amount) {
        console.log(`支付宝支付 ${amount} 元`);
    }
}

class WechatPayStrategy extends PaymentStrategy {
    pay(amount) {
        console.log(`微信支付 ${amount} 元`);
    }
}

class CreditCardStrategy extends PaymentStrategy {
    pay(amount) {
        console.log(`信用卡支付 ${amount} 元`);
    }
}

class PaymentContext {
    constructor(strategy) {
        this.strategy = strategy;
    }

    setStrategy(strategy) {
        this.strategy = strategy;
    }

    pay(amount) {
        this.strategy.pay(amount);
    }
}

// 使用
const context = new PaymentContext(new AlipayStrategy());
context.pay(100);

context.setStrategy(new WechatPayStrategy());
context.pay(200);

// 2. 策略工厂
const strategies = {
    alipay: (amount) => console.log(`支付宝支付 ${amount} 元`),
    wechat: (amount) => console.log(`微信支付 ${amount} 元`),
    creditCard: (amount) => console.log(`信用卡支付 ${amount} 元`),
};

function pay(strategy, amount) {
    const paymentStrategy = strategies[strategy];

    if (!paymentStrategy) {
        throw new Error(`未知的支付策略: ${strategy}`);
    }

    paymentStrategy(amount);
}

// 使用
pay('alipay', 100);
pay('wechat', 200);

// 3. 验证策略
const validationStrategies = {
    required: (value) => {
        return value !== null && value !== undefined && value !== '';
    },
    minLength: (value, min) => {
        return value.length >= min;
    },
    maxLength: (value, max) => {
        return value.length <= max;
    },
    email: (value) => {
        return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);
    },
    phone: (value) => {
        return /^1[3-9]\d{9}$/.test(value);
    },
};

class Validator {
    constructor() {
        this.rules = [];
    }

    addRule(field, strategy, ...args) {
        this.rules.push({
            field,
            strategy,
            args,
        });

        return this;
    }

    validate(data) {
        const errors = [];

        this.rules.forEach((rule) => {
            const { field, strategy, args } = rule;
            const value = data[field];

            const validator = validationStrategies[strategy];

            if (!validator(value, ...args)) {
                errors.push({
                    field,
                    message: `验证失败: ${field}`,
                });
            }
        });

        return {
            valid: errors.length === 0,
            errors,
        };
    }
}

// 使用
const validator = new Validator();

validator
    .addRule('name', 'required')
    .addRule('name', 'minLength', 2)
    .addRule('name', 'maxLength', 50)
    .addRule('email', 'email')
    .addRule('phone', 'phone');

const result = validator.validate({
    name: '张',
    email: 'invalid-email',
    phone: '12345678901',
});

// 4. 排序策略
const sortStrategies = {
    ascending: (a, b) => a - b,
    descending: (a, b) => b - a,
    alphabetical: (a, b) => a.localeCompare(b),
};

function sortArray(array, strategy) {
    const sortFn = sortStrategies[strategy];

    if (!sortFn) {
        throw new Error(`未知的排序策略: ${strategy}`);
    }

    return [...array].sort(sortFn);
}

// 使用
const numbers = [3, 1, 4, 1, 5, 9];
const sorted = sortArray(numbers, 'ascending');

// 5. 缓存策略
const cacheStrategies = {
    memory: () => {
        const cache = new Map();

        return {
            get: (key) => cache.get(key),
            set: (key, value) => cache.set(key, value),
            delete: (key) => cache.delete(key),
            clear: () => cache.clear(),
        };
    },

    localStorage: () => {
        return {
            get: (key) => {
                const value = localStorage.getItem(key);
                return value ? JSON.parse(value) : null;
            },
            set: (key, value) => {
                localStorage.setItem(key, JSON.stringify(value));
            },
            delete: (key) => localStorage.removeItem(key),
            clear: () => localStorage.clear(),
        };
    },

    redis: (redisClient) => {
        return {
            get: async (key) => {
                const value = await redisClient.get(key);
                return value ? JSON.parse(value) : null;
            },
            set: async (key, value, ttl) => {
                await redisClient.set(key, JSON.stringify(value), 'EX', ttl);
            },
            delete: async (key) => {
                await redisClient.del(key);
            },
            clear: async () => {
                await redisClient.flushdb();
            },
        };
    },
};

class CacheManager {
    constructor(strategyName, ...args) {
        const createCache = cacheStrategies[strategyName];

        if (!createCache) {
            throw new Error(`未知的缓存策略: ${strategyName}`);
        }

        this.cache = createCache(...args);
    }

    async get(key) {
        return await this.cache.get(key);
    }

    async set(key, value, ttl) {
        await this.cache.set(key, value, ttl);
    }
}
```

### 4.3 命令模式

```javascript
// 1. 基础命令
class Command {
    execute() {
        throw new Error('必须实现execute方法');
    }

    undo() {
        throw new Error('必须实现undo方法');
    }
}

class AddCommand extends Command {
    constructor(receiver, value) {
        super();
        this.receiver = receiver;
        this.value = value;
    }

    execute() {
        this.receiver.add(this.value);
    }

    undo() {
        this.receiver.remove(this.value);
    }
}

class RemoveCommand extends Command {
    constructor(receiver, value) {
        super();
        this.receiver = receiver;
        this.value = value;
    }

    execute() {
        this.receiver.remove(this.value);
    }

    undo() {
        this.receiver.add(this.value);
    }
}

class Receiver {
    constructor() {
        this.items = [];
    }

    add(item) {
        this.items.push(item);
        console.log('添加:', item);
    }

    remove(item) {
        const index = this.items.indexOf(item);
        if (index > -1) {
            this.items.splice(index, 1);
            console.log('移除:', item);
        }
    }
}

class Invoker {
    constructor() {
        this.history = [];
    }

    execute(command) {
        command.execute();
        this.history.push(command);
    }

    undo() {
        const command = this.history.pop();
        if (command) {
            command.undo();
        }
    }
}

// 使用
const receiver = new Receiver();
const invoker = new Invoker();

invoker.execute(new AddCommand(receiver, 'Item 1'));
invoker.execute(new AddCommand(receiver, 'Item 2'));
invoker.undo();

// 2. 文本编辑器命令
class TextEditor {
    constructor() {
        this.content = '';
    }

    write(text) {
        this.content += text;
    }

    delete(count) {
        this.content = this.content.slice(0, -count);
    }
}

class WriteCommand {
    constructor(editor, text) {
        this.editor = editor;
        this.text = text;
    }

    execute() {
        this.editor.write(this.text);
    }

    undo() {
        this.editor.delete(this.text.length);
    }
}

class DeleteCommand {
    constructor(editor, count) {
        this.editor = editor;
        this.count = count;
        this.deletedText = '';
    }

    execute() {
        const start = this.editor.content.length - this.count;
        this.deletedText = this.editor.content.slice(start);
        this.editor.delete(this.count);
    }

    undo() {
        this.editor.write(this.deletedText);
    }
}

// 3. 宏命令
class MacroCommand {
    constructor() {
        this.commands = [];
    }

    add(command) {
        this.commands.push(command);
    }

    execute() {
        this.commands.forEach((command) => command.execute());
    }

    undo() {
        [...this.commands].reverse().forEach((command) => command.undo());
    }
}

// 4. 异步命令
class AsyncCommand {
    constructor(execute, undo) {
        this.execute = execute;
        this.undo = undo;
    }
}

class AsyncInvoker {
    constructor() {
        this.history = [];
    }

    async execute(command) {
        await command.execute();
        this.history.push(command);
    }

    async undo() {
        const command = this.history.pop();
        if (command) {
            await command.undo();
        }
    }
}

// 使用
const downloadCommand = new AsyncCommand(
    async () => {
        console.log('开始下载');
        await fetch('https://example.com/file');
        console.log('下载完成');
    },
    async () => {
        console.log('取消下载');
    }
);

const invoker = new AsyncInvoker();
await invoker.execute(downloadCommand);
```

---

## 5. JavaScript设计模式

### 5.1 模块模式

```javascript
// 1. 揭示模块模式
const UserModule = (function () {
    const users = [];

    function addUser(user) {
        users.push(user);
    }

    function getUsers() {
        return [...users];
    }

    function findUserById(id) {
        return users.find((user) => user.id === id);
    }

    // 揭示公共API
    return {
        add: addUser,
        getAll: getUsers,
        findById: findUserById,
    };
})();

// 使用
UserModule.add({ id: 1, name: '张三' });
const users = UserModule.getAll();

// 2. ES6模块
// userModule.js
const users = [];

export function addUser(user) {
    users.push(user);
}

export function getUsers() {
    return [...users];
}

export function findUserById(id) {
    return users.find((user) => user.id === id);
}

// 使用
import { addUser, getUsers } from './userModule.js';

// 3. 动态导入
async function loadModule() {
    const module = await import('./heavyModule.js');
    module.doSomething();
}
```

### 5.2 链式调用

```javascript
// 1. jQuery风格链式调用
class DOMBuilder {
    constructor(selector) {
        this.element = document.querySelector(selector);
    }

    addClass(className) {
        this.element.classList.add(className);
        return this;
    }

    removeClass(className) {
        this.element.classList.remove(className);
        return this;
    }

    text(content) {
        this.element.textContent = content;
        return this;
    }

    html(content) {
        this.element.innerHTML = content;
        return this;
    }

    on(event, handler) {
        this.element.addEventListener(event, handler);
        return this;
    }

    css(styles) {
        Object.assign(this.element.style, styles);
        return this;
    }
}

// 使用
const builder = new DOMBuilder('#my-element');

builder
    .addClass('active')
    .text('Hello World')
    .css({ color: 'red', fontSize: '16px' })
    .on('click', () => console.log('Clicked'));

// 2. Promise链式调用
fetch('https://api.example.com/users')
    .then((response) => response.json())
    .then((data) => {
        console.log(data);
        return fetch('https://api.example.com/orders');
    })
    .then((response) => response.json())
    .then((orders) => {
        console.log(orders);
    })
    .catch((error) => {
        console.error('Error:', error);
    });

// 3. 自定义链式调用
class Calculator {
    constructor(initialValue = 0) {
        this.value = initialValue;
    }

    add(num) {
        this.value += num;
        return this;
    }

    subtract(num) {
        this.value -= num;
        return this;
    }

    multiply(num) {
        this.value *= num;
        return this;
    }

    divide(num) {
        this.value /= num;
        return this;
    }

    getResult() {
        return this.value;
    }
}

// 使用
const result = new Calculator(10)
    .add(5)
    .multiply(2)
    .subtract(3)
    .getResult();

console.log(result);  // 27
```

---

## 6. React设计模式

### 6.1 复合组件模式

```javascript
// 1. Tabs复合组件
const TabsContext = React.createContext();

function Tabs({ children, defaultIndex = 0 }) {
    const [activeIndex, setActiveIndex] = useState(defaultIndex);

    return (
        <TabsContext.Provider value={{ activeIndex, setActiveIndex }}>
            {children}
        </TabsContext.Provider>
    );
}

function TabList({ children }) {
    return <div className="tab-list">{children}</div>;
}

function Tab({ index, children }) {
    const { activeIndex, setActiveIndex } = useContext(TabsContext);
    const isActive = activeIndex === index;

    return (
        <button
            className={isActive ? 'tab active' : 'tab'}
            onClick={() => setActiveIndex(index)}
        >
            {children}
        </button>
    );
}

function TabPanels({ children }) {
    return <div className="tab-panels">{children}</div>;
}

function TabPanel({ index, children }) {
    const { activeIndex } = useContext(TabsContext);

    if (activeIndex !== index) {
        return null;
    }

    return <div className="tab-panel">{children}</div>;
}

// 使用
<Tabs defaultIndex={0}>
    <Tabs.TabList>
        <Tabs.Tab index={0}>标签1</Tabs.Tab>
        <Tabs.Tab index={1}>标签2</Tabs.Tab>
    </Tabs.TabList>
    <Tabs.TabPanels>
        <Tabs.TabPanel index={0}>内容1</Tabs.TabPanel>
        <Tabs.TabPanel index={1}>内容2</Tabs.TabPanel>
    </Tabs.TabPanels>
</Tabs>

// 2. Render Props模式
class Mouse extends React.Component {
    state = { x: 0, y: 0 };

    handleMouseMove = (event) => {
        this.setState({
            x: event.clientX,
            y: event.clientY,
        });
    };

    render() {
        return (
            <div onMouseMove={this.handleMouseMove}>
                {this.props.render(this.state)}
            </div>
        );
    }
}

// 使用
<Mouse
    render={({ x, y }) => (
        <h1>
            鼠标位置: {x}, {y}
        </h1>
    )}
/>

// 3. 高阶组件（HOC）
function withAuth(WrappedComponent) {
    return function WithAuthComponent(props) {
        const { user, isLoading } = useAuth();

        if (isLoading) {
            return <div>加载中...</div>;
        }

        if (!user) {
            return <Redirect to="/login" />;
        }

        return <WrappedComponent {...props} user={user} />;
    };
}

// 使用
const ProtectedPage = withAuth(Page);

// 4. 自定义Hook模式
function useLocalStorage(key, initialValue) {
    const [storedValue, setStoredValue] = useState(() => {
        try {
            const item = localStorage.getItem(key);
            return item ? JSON.parse(item) : initialValue;
        } catch (error) {
            return initialValue;
        }
    });

    const setValue = (value) => {
        try {
            const valueToStore = value instanceof Function ? value(storedValue) : value;
            setStoredValue(valueToStore);
            localStorage.setItem(key, JSON.stringify(valueToStore));
        } catch (error) {
            console.error(error);
        }
    };

    return [storedValue, setValue];
}

// 使用
const [name, setName] = useLocalStorage('name', '张三');

// 5. 受控组件与非受控组件
// 受控组件
function ControlledInput() {
    const [value, setValue] = useState('');

    return (
        <input
            type="text"
            value={value}
            onChange={(e) => setValue(e.target.value)}
        />
    );
}

// 非受控组件
function UncontrolledInput() {
    const inputRef = useRef();

    const handleSubmit = () => {
        console.log(inputRef.current.value);
    };

    return (
        <>
            <input type="text" ref={inputRef} defaultValue="初始值" />
            <button onClick={handleSubmit}>提交</button>
        </>
    );
}
```

---

## 参考资源

- [设计模式：可复用面向对象软件的基础](https://www.oreilly.com/library/view/design-patterns-elements/0201633612/)
- [JavaScript设计模式](https://www.patterns.dev/)
- [React设计模式](https://reactpatterns.com/)
- [Refactoring Guru](https://refactoring.guru/design-patterns)

---

*本文档持续更新，最后更新于2026年3月*