# 全栈框架对比与选型指南（2026版）

## 一、全栈框架概览

### 1.1 全栈框架分类

| 分类 | 代表框架 | 核心特点 | 适用场景 |
|------|----------|----------|----------|
| **传统分离式** | Express + React | 前后端分离，灵活度高 | 中小型项目、快速迭代 |
| **全栈同构** | Next.js 16 | 同构渲染，SEO友好 | 内容型网站、电商、SEO敏感项目 |
| **微服务架构** | NestJS + React | 模块化、可扩展 | 大型项目、团队协作、微服务 |
| **边缘计算** | Hono + Cloudflare | 轻量、边缘部署 | Serverless、边缘计算 |

### 1.2 2026年技术栈推荐

```
┌─────────────────────────────────────────────────────────────┐
│                    2026全栈技术栈推荐                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  小型项目（1-3人团队）                                       │
│  └─ 前端：React 19 + TypeScript + Tailwind CSS             │
│  └─ 后端：Express + MongoDB                                │
│  └─ 部署：Vercel / Netlify                                 │
│                                                             │
│  中型项目（3-10人团队）                                      │
│  └─ 前端：Next.js 16 + TypeScript + Zustand                │
│  └─ 后端：NestJS + PostgreSQL + Redis                      │
│  └─ 部署：Vercel + Render / 自建K8s                        │
│                                                             │
│  大型项目（10+人团队）                                       │
│  └─ 前端：Next.js 16 + 微前端 + 性能监控                   │
│  └─ 后端：NestJS微服务 + gRPC + 分布式缓存                 │
│  └─ 部署：Kubernetes + CI/CD + 监控告警                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、Express框架详解

### 2.1 核心特性

| 特性 | 说明 | 优势 |
|------|------|------|
| **极简设计** | 核心功能精简，按需扩展 | 学习成本低，上手快 |
| **中间件生态** | 丰富的第三方中间件 | 功能扩展灵活 |
| **灵活配置** | 高度可定制 | 适应各种需求 |
| **社区活跃** | 大量教程和资源 | 问题易解决 |

### 2.2 项目结构

```
express-project/
├── src/
│   ├── routes/          # 路由定义
│   │   ├── index.ts
│   │   ├── users.ts
│   │   └── products.ts
│   ├── controllers/     # 控制器
│   │   ├── userController.ts
│   │   └── productController.ts
│   ├── services/        # 业务逻辑
│   │   ├── userService.ts
│   │   └── productService.ts
│   ├── middleware/      # 自定义中间件
│   │   ├── auth.ts
│   │   └── error.ts
│   ├── models/          # 数据模型
│   │   └── user.ts
│   └── config/          # 配置文件
│       ├── database.ts
│       └── env.ts
├── tests/               # 测试文件
├── package.json
└── tsconfig.json
```

### 2.3 代码示例

**基础服务器：**
```typescript
// src/app.ts
import express, { Application, Request, Response, NextFunction } from 'express';
import cors from 'cors';
import helmet from 'helmet';
import morgan from 'morgan';

// 创建应用
const app: Application = express();

// 中间件配置
app.use(helmet()); // 安全头
app.use(cors()); // 跨域
app.use(express.json()); // JSON解析
app.use(express.urlencoded({ extended: true })); // URL编码
app.use(morgan('dev')); // 日志

// 健康检查
app.get('/health', (req: Request, res: Response) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

// 错误处理中间件
app.use((err: Error, req: Request, res: Response, next: NextFunction) => {
  console.error('错误:', err.message);
  res.status(500).json({ error: '服务器内部错误' });
});

// 启动服务器
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`服务器运行在 http://localhost:${PORT}`);
});
```

**路由示例：**
```typescript
// src/routes/users.ts
import { Router, Request, Response } from 'express';
import * as userController from '../controllers/userController';

const router = Router();

// 获取用户列表
router.get('/', async (req: Request, res: Response) => {
  try {
    const users = await userController.getAllUsers();
    res.json(users);
  } catch (error) {
    res.status(500).json({ error: '获取用户失败' });
  }
});

// 获取单个用户
router.get('/:id', async (req: Request, res: Response) => {
  try {
    const user = await userController.getUserById(req.params.id);
    if (!user) {
      return res.status(404).json({ error: '用户不存在' });
    }
    res.json(user);
  } catch (error) {
    res.status(500).json({ error: '获取用户失败' });
  }
});

// 创建用户
router.post('/', async (req: Request, res: Response) => {
  try {
    const user = await userController.createUser(req.body);
    res.status(201).json(user);
  } catch (error) {
    res.status(500).json({ error: '创建用户失败' });
  }
});

export default router;
```

**中间件示例：**
```typescript
// src/middleware/auth.ts
import { Request, Response, NextFunction } from 'express';
import jwt from 'jsonwebtoken';

// TypeScript类型扩展
declare global {
  namespace Express {
    interface Request {
      user?: {
        id: string;
        email: string;
        role: string;
      };
    }
  }
}

// 验证JWT中间件
export const authenticate = (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  const authHeader = req.headers.authorization;
  
  if (!authHeader || !authHeader.startsWith('Bearer ')) {
    return res.status(401).json({ error: '未提供认证令牌' });
  }

  const token = authHeader.split(' ')[1];

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET!);
    req.user = decoded as { id: string; email: string; role: string };
    next();
  } catch (error) {
    return res.status(401).json({ error: '令牌无效或已过期' });
  }
};

// 授权中间件
export const authorize = (roles: string[]) => {
  return (req: Request, res: Response, next: NextFunction) => {
    if (!req.user) {
      return res.status(401).json({ error: '未认证' });
    }

    if (!roles.includes(req.user.role)) {
      return res.status(403).json({ error: '无权限访问' });
    }

    next();
  };
};
```

### 2.4 最佳实践

| 实践 | 说明 |
|------|------|
| **使用TypeScript** | 类型安全，减少运行时错误 |
| **中间件分离** | 业务逻辑与路由分离 |
| **错误处理** | 统一错误处理中间件 |
| **日志记录** | 使用morgan或winston记录请求日志 |
| **安全头** | 使用helmet设置安全头 |
| **环境变量** | 使用dotenv管理环境变量 |
| **验证中间件** | 使用joi或zod进行请求验证 |
| **API版本控制** | 在URL中包含版本号 |

---

## 三、Next.js 16框架详解

### 3.1 核心特性

| 特性 | 说明 | 优势 |
|------|------|------|
| **App Router** | 基于文件系统的路由 | 简洁直观，支持嵌套路由 |
| **Server Components** | 默认服务端组件 | 零客户端JavaScript，性能优秀 |
| **Server Actions** | 服务端操作 | 无需API路由，简化数据提交 |
| **Turbopack** | Rust编写的构建工具 | 极速编译，开发体验优秀 |
| **流式渲染** | 渐进式页面加载 | 更好的用户体验 |
| **SEO优化** | 内置元数据管理 | 搜索引擎友好 |

### 3.2 项目结构

```
nextjs-project/
├── app/                 # App Router目录
│   ├── layout.tsx       # 根布局
│   ├── page.tsx         # 首页
│   ├── loading.tsx      # 加载状态
│   ├── error.tsx        # 错误边界
│   ├── not-found.tsx    # 404页面
│   ├── page.tsx         # 动态路由
│   ├── @modal/          # 并行路由
│   │   └── page.tsx
│   └── api/             # API路由
│       └── route.ts
├── src/                 # 源代码
│   ├── components/      # 组件
│   ├── lib/             # 工具函数
│   └── types/           # 类型定义
├── public/              # 静态资源
├── styles/              # 样式文件
├── next.config.js       # Next.js配置
└── tsconfig.json
```

### 3.3 代码示例

**基础页面：**
```typescript
// app/page.tsx
import Link from 'next/link';
import { Metadata } from 'next';

// 元数据配置
export const metadata: Metadata = {
  title: '首页 - Next.js 16全栈应用',
  description: '基于Next.js 16构建的全栈应用',
  openGraph: {
    title: 'Next.js 16全栈应用',
    description: '基于Next.js 16构建的全栈应用',
    url: 'https://example.com',
    siteName: 'Next.js 16全栈应用',
    locale: 'zh-CN',
    type: 'website',
  },
};

// 静态生成
export default function HomePage() {
  return (
    <main className="min-h-screen bg-gray-50">
      <header className="bg-white shadow-sm">
        <div className="container mx-auto px-4 py-4">
          <h1 className="text-3xl font-bold text-gray-900">
            Next.js 16全栈应用
          </h1>
          <nav className="mt-4 flex space-x-4">
            <Link href="/about" className="text-blue-600 hover:text-blue-800">
              关于
            </Link>
            <Link href="/contact" className="text-blue-600 hover:text-blue-800">
              联系
            </Link>
          </nav>
        </div>
      </header>
      
      <div className="container mx-auto px-4 py-8">
        <div className="bg-white rounded-lg shadow p-6">
          <h2 className="text-2xl font-semibold mb-4">欢迎使用Next.js 16</h2>
          <p className="text-gray-600">
            Next.js 16提供了完整的全栈开发解决方案，包括服务端渲染、静态生成、API路由等功能。
          </p>
        </div>
      </div>
    </main>
  );
}
```

**动态路由：**
```typescript
// app/users/[id]/page.tsx
import { notFound } from 'next/navigation';
import { Metadata } from 'next';

// 生成静态路径
export async function generateStaticParams() {
  const users = await fetch('https://jsonplaceholder.typicode.com/users')
    .then(res => res.json());
  
  return users.map((user: { id: number }) => ({
    id: user.id.toString(),
  }));
}

// 元数据生成
export async function generateMetadata({ params }: { params: { id: string } }): Promise<Metadata> {
  const user = await fetch(`https://jsonplaceholder.typicode.com/users/${params.id}`)
    .then(res => res.json());
  
  return {
    title: `${user.name} - 用户详情`,
    description: `查看${user.name}的详细信息`,
  };
}

// 动态页面
export default async function UserPage({ params }: { params: { id: string } }) {
  const user = await fetch(`https://jsonplaceholder.typicode.com/users/${params.id}`)
    .then(res => res.json())
    .catch(() => notFound());

  return (
    <div className="container mx-auto px-4 py-8">
      <div className="bg-white rounded-lg shadow p-6">
        <h1 className="text-3xl font-bold mb-4">{user.name}</h1>
        <div className="space-y-2">
          <p><strong>邮箱:</strong> {user.email}</p>
          <p><strong>电话:</strong> {user.phone}</p>
          <p><strong>网站:</strong> {user.website}</p>
          <p><strong>公司:</strong> {user.company.name}</p>
        </div>
        <Link href="/users" className="mt-4 inline-block text-blue-600 hover:underline">
          ← 返回用户列表
        </Link>
      </div>
    </div>
  );
}
```

**Server Actions：**
```typescript
// app/actions.ts
'use server';

import { revalidatePath } from 'next/cache';
import { redirect } from 'next/navigation';

// 创建用户
export async function createUser(formData: FormData) {
  const name = formData.get('name') as string;
  const email = formData.get('email') as string;

  try {
    const response = await fetch('https://jsonplaceholder.typicode.com/users', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name, email }),
    });

    if (!response.ok) {
      throw new Error('创建用户失败');
    }

    revalidatePath('/users');
    redirect('/users');
  } catch (error) {
    return { error: '创建用户失败' };
  }
}

// 删除用户
export async function deleteUser(id: string) {
  try {
    await fetch(`https://jsonplaceholder.typicode.com/users/${id}`, {
      method: 'DELETE',
    });

    revalidatePath('/users');
  } catch (error) {
    console.error('删除用户失败:', error);
  }
}
```

**API路由：**
```typescript
// app/api/users/route.ts
import { NextRequest, NextResponse } from 'next/server';

// 获取用户列表
export async function GET() {
  try {
    const response = await fetch('https://jsonplaceholder.typicode.com/users');
    const users = await response.json();
    
    return NextResponse.json(users);
  } catch (error) {
    return NextResponse.json({ error: '获取用户失败' }, { status: 500 });
  }
}

// 创建用户
export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { name, email } = body;

    if (!name || !email) {
      return NextResponse.json({ error: '缺少必要字段' }, { status: 400 });
    }

    const response = await fetch('https://jsonplaceholder.typicode.com/users', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name, email }),
    });

    const user = await response.json();
    return NextResponse.json(user, { status: 201 });
  } catch (error) {
    return NextResponse.json({ error: '创建用户失败' }, { status: 500 });
  }
}
```

**布局与并行路由：**
```typescript
// app/layout.tsx
import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Next.js 16全栈应用',
  description: '基于Next.js 16构建的全栈应用',
};

export default function RootLayout({
  children,
  modal,
}: {
  children: React.ReactNode;
  modal: React.ReactNode;
}) {
  return (
    <html lang="zh-CN">
      <body className={inter.className}>
        {children}
        {modal}
      </body>
    </html>
  );
}

// app/@modal/page.tsx
export default function Modal() {
  return (
    <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
      <div className="bg-white rounded-lg p-6 max-w-md">
        <h2 className="text-xl font-bold mb-4">模态框</h2>
        <p className="text-gray-600">这是并行路由的模态框内容</p>
      </div>
    </div>
  );
}
```

### 3.4 最佳实践

| 实践 | 说明 |
|------|------|
| **使用App Router** | 新项目使用App Router，替代Pages Router |
| **Server Components优先** | 默认使用服务端组件 |
| **流式渲染** | 使用Suspense实现渐进式加载 |
| **图片优化** | 使用next/image组件 |
| **字体优化** | 使用next/font优化字体加载 |
| **元数据管理** | 使用metadata导出配置SEO |
| **API路由** | 使用app/api目录创建API |
| **缓存策略** | 使用cache和revalidate控制缓存 |

---

## 四、NestJS框架详解

### 4.1 核心特性

| 特性 | 说明 | 优势 |
|------|------|------|
| **TypeScript原生** | 基于TypeScript开发 | 类型安全，IDE支持好 |
| **模块化架构** | 基于模块的架构 | 代码组织清晰，易于维护 |
| **依赖注入** | 内置DI容器 | 解耦合，易于测试 |
| **装饰器语法** | 类似Angular的装饰器 | 代码简洁，易于理解 |
| **中间件系统** | 完善的中间件系统 | 功能扩展灵活 |
| **企业级特性** | 完整的企业级特性 | 适合大型项目 |

### 4.2 项目结构

```
nestjs-project/
├── src/
│   ├── app.module.ts          # 根模块
│   ├── main.ts                # 入口文件
│   ├── common/                # 公共模块
│   │   ├── filters/           # 异常过滤器
│   │   ├── guards/            # 守卫
│   │   ├── interceptors/      # 拦截器
│   │   ├── pipes/             # 管道
│   │   └── decorators/        # 自定义装饰器
│   ├── modules/               # 业务模块
│   │   ├── users/             # 用户模块
│   │   ├── auth/              # 认证模块
│   │   └── products/          # 产品模块
│   ├── entities/              # 数据实体
│   └── config/                # 配置
├── test/                      # 测试文件
├── nest-cli.json              # NestJS CLI配置
└── tsconfig.json
```

### 4.3 代码示例

**基础应用：**
```typescript
// src/main.ts
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';
import { ValidationPipe } from '@nestjs/common';
import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  // 全局前缀
  app.setGlobalPrefix('api');

  // 全局验证管道
  app.useGlobalPipes(new ValidationPipe({
    whitelist: true, // 移除未定义的属性
    transform: true, // 自动转换类型
    forbidNonWhitelisted: true, // 禁止非白名单属性
  }));

  // Swagger文档
  const config = new DocumentBuilder()
    .setTitle('NestJS API文档')
    .setDescription('NestJS全栈应用API文档')
    .setVersion('1.0')
    .addBearerAuth()
    .build();
  
  const document = SwaggerModule.createDocument(app, config);
  SwaggerModule.setup('api/docs', app, document);

  // 跨域
  app.enableCors();

  // 启动服务器
  const PORT = process.env.PORT || 3000;
  await app.listen(PORT);
  console.log(`服务器运行在 http://localhost:${PORT}`);
  console.log(`API文档访问 http://localhost:${PORT}/api/docs`);
}

bootstrap();
```

**模块与依赖注入：**
```typescript
// src/modules/users/users.module.ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { User } from '../../entities/user.entity';
import { UsersController } from './users.controller';
import { UsersService } from './users.service';

@Module({
  imports: [
    TypeOrmModule.forFeature([User]), // 注册实体
  ],
  controllers: [UsersController], // 控制器
  providers: [UsersService], // 服务
  exports: [UsersService], // 导出服务
})
export class UsersModule {}
```

```typescript
// src/modules/users/users.service.ts
import { Injectable, NotFoundException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { User } from '../../entities/user.entity';
import { CreateUserDto } from './dto/create-user.dto';
import { UpdateUserDto } from './dto/update-user.dto';

@Injectable()
export class UsersService {
  constructor(
    @InjectRepository(User)
    private readonly userRepository: Repository<User>,
  ) {}

  // 获取所有用户
  async findAll(): Promise<User[]> {
    return await this.userRepository.find();
  }

  // 获取单个用户
  async findOne(id: string): Promise<User> {
    const user = await this.userRepository.findOneBy({ id });
    if (!user) {
      throw new NotFoundException(`用户 ${id} 不存在`);
    }
    return user;
  }

  // 创建用户
  async create(createUserDto: CreateUserDto): Promise<User> {
    const user = this.userRepository.create(createUserDto);
    return await this.userRepository.save(user);
  }

  // 更新用户
  async update(id: string, updateUserDto: UpdateUserDto): Promise<User> {
    const user = await this.findOne(id);
    Object.assign(user, updateUserDto);
    return await this.userRepository.save(user);
  }

  // 删除用户
  async remove(id: string): Promise<void> {
    const user = await this.findOne(id);
    await this.userRepository.remove(user);
  }
}
```

**控制器与装饰器：**
```typescript
// src/modules/users/users.controller.ts
import {
  Controller,
  Get,
  Post,
  Put,
  Delete,
  Param,
  Body,
  Query,
  Headers,
  UseGuards,
  UseInterceptors,
  UploadedFile,
} from '@nestjs/common';
import { UsersService } from './users.service';
import { CreateUserDto } from './dto/create-user.dto';
import { UpdateUserDto } from './dto/update-user.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { FileInterceptor } from '@nestjs/platform-express';

@Controller('users')
@UseGuards(JwtAuthGuard) // 全局守卫
export class UsersController {
  constructor(private readonly usersService: UsersService) {}

  // 获取所有用户
  @Get()
  findAll() {
    return this.usersService.findAll();
  }

  // 获取单个用户
  @Get(':id')
  findOne(@Param('id') id: string) {
    return this.usersService.findOne(id);
  }

  // 创建用户
  @Post()
  create(@Body() createUserDto: CreateUserDto) {
    return this.usersService.create(createUserDto);
  }

  // 更新用户
  @Put(':id')
  update(@Param('id') id: string, @Body() updateUserDto: UpdateUserDto) {
    return this.usersService.update(id, updateUserDto);
  }

  // 删除用户
  @Delete(':id')
  remove(@Param('id') id: string) {
    return this.usersService.remove(id);
  }

  // 查询参数
  @Get('search')
  search(@Query('keyword') keyword: string) {
    return this.usersService.search(keyword);
  }

  // 请求头
  @Get('headers')
  getHeaders(@Headers('authorization') authHeader: string) {
    return { authorization: authHeader };
  }

  // 文件上传
  @Post('upload')
  @UseInterceptors(FileInterceptor('file'))
  uploadFile(@UploadedFile() file: Express.Multer.File) {
    return { filename: file.filename, size: file.size };
  }
}
```

**守卫与认证：**
```typescript
// src/modules/auth/guards/jwt-auth.guard.ts
import { Injectable } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {}
```

```typescript
// src/modules/auth/guards/local-auth.guard.ts
import { Injectable } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';

@Injectable()
export class LocalAuthGuard extends AuthGuard('local') {}
```

**异常过滤器：**
```typescript
// src/common/filters/http-exception.filter.ts
import {
  ExceptionFilter,
  Catch,
  ArgumentsHost,
  HttpException,
  HttpStatus,
} from '@nestjs/common';
import { Response } from 'express';

@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  catch(exception: any, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();

    // 处理TypeORM错误
    if (exception.code === 'ER_DUP_ENTRY') {
      return response.status(HttpStatus.CONFLICT).json({
        statusCode: HttpStatus.CONFLICT,
        message: '数据已存在',
        error: 'Conflict',
      });
    }

    // 处理TypeORM NotFoundError
    if (exception.name === 'EntityNotFoundError') {
      return response.status(HttpStatus.NOT_FOUND).json({
        statusCode: HttpStatus.NOT_FOUND,
        message: '资源不存在',
        error: 'Not Found',
      });
    }

    // 处理JWT错误
    if (exception.name === 'JsonWebTokenError') {
      return response.status(HttpStatus.UNAUTHORIZED).json({
        statusCode: HttpStatus.UNAUTHORIZED,
        message: '令牌无效',
        error: 'Unauthorized',
      });
    }

    // 处理JWT过期错误
    if (exception.name === 'TokenExpiredError') {
      return response.status(HttpStatus.UNAUTHORIZED).json({
        statusCode: HttpStatus.UNAUTHORIZED,
        message: '令牌已过期',
        error: 'Unauthorized',
      });
    }

    // 处理HTTP异常
    if (exception instanceof HttpException) {
      return response.status(exception.getStatus()).json(exception.getResponse());
    }

    // 处理未知异常
    return response.status(HttpStatus.INTERNAL_SERVER_ERROR).json({
      statusCode: HttpStatus.INTERNAL_SERVER_ERROR,
      message: '服务器内部错误',
      error: 'Internal Server Error',
    });
  }
}
```

**拦截器：**
```typescript
// src/common/interceptors/logging.interceptor.ts
import {
  Injectable,
  NestInterceptor,
  ExecutionContext,
  CallHandler,
} from '@nestjs/common';
import { Observable } from 'rxjs';
import { tap } from 'rxjs/operators';

@Injectable()
export class LoggingInterceptor implements NestInterceptor {
  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
    const now = Date.now();
    const request = context.switchToHttp().getRequest();
    
    console.log(`请求开始: ${request.method} ${request.url}`);

    return next.handle().pipe(
      tap(() => {
        const duration = Date.now() - now;
        console.log(`请求结束: ${request.method} ${request.url} - ${duration}ms`);
      }),
    );
  }
}
```

### 4.4 最佳实践

| 实践 | 说明 |
|------|------|
| **模块化设计** | 按功能划分模块，每个模块职责单一 |
| **依赖注入** | 使用DI容器管理依赖，便于测试 |
| **守卫与拦截器** | 使用守卫处理认证授权，使用拦截器处理日志等 |
| **异常过滤器** | 统一异常处理，返回一致的错误格式 |
| **DTO验证** | 使用DTO进行请求验证，确保数据安全 |
| **TypeORM实体** | 使用TypeORM管理数据库实体 |
| **Swagger文档** | 使用Swagger生成API文档 |
| **单元测试** | 编写单元测试，确保代码质量 |

---

## 五、框架对比与选型

### 5.1 对比矩阵

| 特性 | Express | Next.js 16 | NestJS |
|------|---------|------------|--------|
| **学习曲线** | ⭐ 低 | ⭐⭐ 中 | ⭐⭐⭐ 高 |
| **开发效率** | ⭐⭐⭐ 高 | ⭐⭐⭐⭐ 很高 | ⭐⭐⭐ 高 |
| **可维护性** | ⭐⭐ 中 | ⭐⭐⭐⭐ 很高 | ⭐⭐⭐⭐⭐ 优秀 |
| **性能** | ⭐⭐⭐ 中 | ⭐⭐⭐⭐ 高 | ⭐⭐⭐⭐ 高 |
| **TypeScript支持** | ⭐⭐ 需配置 | ⭐⭐⭐⭐⭐ 原生 | ⭐⭐⭐⭐⭐ 原生 |
| **SEO优化** | ⭐ 无 | ⭐⭐⭐⭐⭐ 优秀 | ⭐ 无 |
| **SSR支持** | ⭐ 无 | ⭐⭐⭐⭐⭐ 优秀 | ⭐ 无 |
| **微服务支持** | ⭐⭐ 需扩展 | ⭐⭐⭐ 中 | ⭐⭐⭐⭐⭐ 优秀 |
| **企业级特性** | ⭐⭐ 需扩展 | ⭐⭐⭐ 中 | ⭐⭐⭐⭐⭐ 优秀 |
| **社区生态** | ⭐⭐⭐⭐⭐ 丰富 | ⭐⭐⭐⭐⭐ 丰富 | ⭐⭐⭐⭐ 丰富 |

### 5.2 选型建议

#### 小型项目（1-3人团队）

**推荐技术栈：**
```
前端：React 19 + TypeScript + Tailwind CSS
后端：Express + MongoDB
部署：Vercel / Netlify
```

**选型理由：**
- Express学习成本低，快速上手
- MongoDB文档数据库，开发灵活
- Vercel/Netlify一键部署，无需运维

**适用场景：**
- 个人项目
- MVP快速验证
- 内容型网站
- 小型电商

#### 中型项目（3-10人团队）

**推荐技术栈：**
```
前端：Next.js 16 + TypeScript + Zustand
后端：NestJS + PostgreSQL + Redis
部署：Vercel + Render / 自建K8s
```

**选型理由：**
- Next.js提供SSR和SEO优化
- NestJS提供企业级架构
- PostgreSQL关系型数据库，数据一致性好
- Redis缓存，提升性能

**适用场景：**
- 企业官网
- 电商平台
- SaaS应用
- 内容管理系统

#### 大型项目（10+人团队）

**推荐技术栈：**
```
前端：Next.js 16 + 微前端 + 性能监控
后端：NestJS微服务 + gRPC + 分布式缓存
部署：Kubernetes + CI/CD + 监控告警
```

**选型理由：**
- 微前端实现团队独立开发
- NestJS微服务实现业务隔离
- gRPC高性能服务间通信
- Kubernetes容器编排

**适用场景：**
- 复杂电商平台
- 企业级SaaS
- 多团队协作项目
- 高并发应用

### 5.3 迁移指南

#### Express迁移到NestJS

**步骤1：创建NestJS项目**
```bash
npm i -g @nestjs/cli
nest new my-project
```

**步骤2：迁移路由**
```typescript
// Express
app.get('/users', getUsers);

// NestJS
@Controller('users')
export class UsersController {
  @Get()
  getAllUsers() {
    return this.usersService.getAllUsers();
  }
}
```

**步骤3：迁移中间件**
```typescript
// Express
app.use(cors());

// NestJS
app.enableCors();
```

#### Express迁移到Next.js

**步骤1：创建Next.js项目**
```bash
npx create-next-app@latest my-app
```

**步骤2：迁移API路由**
```typescript
// Express
app.get('/api/users', (req, res) => {
  res.json(users);
});

// Next.js
// app/api/users/route.ts
export async function GET() {
  return NextResponse.json(users);
}
```

**步骤3：迁移页面路由**
```typescript
// Express
app.get('/users', (req, res) => {
  res.render('users', { users });
});

// Next.js
// app/users/page.tsx
export default function UsersPage() {
  return <div>用户列表</div>;
}
```

---

## 六、实战案例

### 6.1 Todo应用（Express）

**项目结构：**
```
todo-express/
├── src/
│   ├── routes/
│   │   └── todos.ts
│   ├── controllers/
│   │   └── todoController.ts
│   ├── models/
│   │   └── todo.ts
│   └── app.ts
└── package.json
```

**代码实现：**
```typescript
// src/models/todo.ts
export interface Todo {
  id: string;
  title: string;
  completed: boolean;
  createdAt: Date;
}

// src/controllers/todoController.ts
import { Request, Response } from 'express';
import { Todo } from '../models/todo';

const todos: Todo[] = [];

export const getAllTodos = (req: Request, res: Response) => {
  res.json(todos);
};

export const createTodo = (req: Request, res: Response) => {
  const { title } = req.body;
  const todo: Todo = {
    id: Date.now().toString(),
    title,
    completed: false,
    createdAt: new Date(),
  };
  todos.push(todo);
  res.status(201).json(todo);
};

export const updateTodo = (req: Request, res: Response) => {
  const { id } = req.params;
  const { title, completed } = req.body;
  
  const todo = todos.find(t => t.id === id);
  if (!todo) {
    return res.status(404).json({ error: 'Todo not found' });
  }

  todo.title = title || todo.title;
  todo.completed = completed !== undefined ? completed : todo.completed;
  
  res.json(todo);
};

export const deleteTodo = (req: Request, res: Response) => {
  const { id } = req.params;
  const index = todos.findIndex(t => t.id === id);
  
  if (index === -1) {
    return res.status(404).json({ error: 'Todo not found' });
  }

  todos.splice(index, 1);
  res.status(204).send();
};
```

### 6.2 Todo应用（Next.js）

**项目结构：**
```
todo-nextjs/
├── app/
│   ├── api/
│   │   └── todos/
│   │       └── route.ts
│   ├── page.tsx
│   └── layout.tsx
└── package.json
```

**代码实现：**
```typescript
// app/api/todos/route.ts
import { NextRequest, NextResponse } from 'next/server';

interface Todo {
  id: string;
  title: string;
  completed: boolean;
  createdAt: Date;
}

const todos: Todo[] = [];

export async function GET() {
  return NextResponse.json(todos);
}

export async function POST(request: NextRequest) {
  const body = await request.json();
  const { title } = body;

  const todo: Todo = {
    id: Date.now().toString(),
    title,
    completed: false,
    createdAt: new Date(),
  };

  todos.push(todo);
  return NextResponse.json(todo, { status: 201 });
}

export async function PUT(request: NextRequest) {
  const body = await request.json();
  const { id, title, completed } = body;

  const todo = todos.find(t => t.id === id);
  if (!todo) {
    return NextResponse.json({ error: 'Todo not found' }, { status: 404 });
  }

  todo.title = title || todo.title;
  todo.completed = completed !== undefined ? completed : todo.completed;

  return NextResponse.json(todo);
}

export async function DELETE(request: NextRequest) {
  const body = await request.json();
  const { id } = body;

  const index = todos.findIndex(t => t.id === id);
  if (index === -1) {
    return NextResponse.json({ error: 'Todo not found' }, { status: 404 });
  }

  todos.splice(index, 1);
  return NextResponse.json({ message: 'Deleted' });
}
```

### 6.3 Todo应用（NestJS）

**项目结构：**
```
todo-nestjs/
├── src/
│   ├── todos/
│   │   ├── todos.controller.ts
│   │   ├── todos.service.ts
│   │   └── todos.module.ts
│   └── main.ts
└── package.json
```

**代码实现：**
```typescript
// src/todos/dto/create-todo.dto.ts
import { IsString, IsOptional } from 'class-validator';

export class CreateTodoDto {
  @IsString()
  title: string;

  @IsOptional()
  @IsString()
  description?: string;
}
```

```typescript
// src/todos/todos.service.ts
import { Injectable } from '@nestjs/common';
import { CreateTodoDto } from './dto/create-todo.dto';
import { UpdateTodoDto } from './dto/update-todo.dto';

interface Todo {
  id: string;
  title: string;
  description?: string;
  completed: boolean;
  createdAt: Date;
}

@Injectable()
export class TodosService {
  private todos: Todo[] = [];

  create(createTodoDto: CreateTodoDto): Todo {
    const todo: Todo = {
      id: Date.now().toString(),
      title: createTodoDto.title,
      description: createTodoDto.description,
      completed: false,
      createdAt: new Date(),
    };
    this.todos.push(todo);
    return todo;
  }

  findAll(): Todo[] {
    return this.todos;
  }

  findOne(id: string): Todo | undefined {
    return this.todos.find(t => t.id === id);
  }

  update(id: string, updateTodoDto: UpdateTodoDto): Todo | undefined {
    const todo = this.findOne(id);
    if (!todo) return undefined;

    Object.assign(todo, updateTodoDto);
    return todo;
  }

  remove(id: string): boolean {
    const index = this.todos.findIndex(t => t.id === id);
    if (index === -1) return false;
    this.todos.splice(index, 1);
    return true;
  }
}
```

```typescript
// src/todos/todos.controller.ts
import {
  Controller,
  Get,
  Post,
  Body,
  Param,
  Put,
  Delete,
} from '@nestjs/common';
import { TodosService } from './todos.service';
import { CreateTodoDto } from './dto/create-todo.dto';
import { UpdateTodoDto } from './dto/update-todo.dto';

@Controller('todos')
export class TodosController {
  constructor(private readonly todosService: TodosService) {}

  @Post()
  create(@Body() createTodoDto: CreateTodoDto) {
    return this.todosService.create(createTodoDto);
  }

  @Get()
  findAll() {
    return this.todosService.findAll();
  }

  @Get(':id')
  findOne(@Param('id') id: string) {
    return this.todosService.findOne(id);
  }

  @Put(':id')
  update(
    @Param('id') id: string,
    @Body() updateTodoDto: UpdateTodoDto,
  ) {
    return this.todosService.update(id, updateTodoDto);
  }

  @Delete(':id')
  remove(@Param('id') id: string) {
    return this.todosService.remove(id);
  }
}
```

---

## 七、总结

### 7.1 框架选型决策树

```
项目需求
├─ 小型项目（1-3人）
│  ├─ 快速开发 → Express + MongoDB + Vercel
│  └─ SEO重要 → Next.js + PostgreSQL + Vercel
│
├─ 中型项目（3-10人）
│  ├─ 内容型 → Next.js + NestJS + PostgreSQL
│  └─ API型 → NestJS + PostgreSQL + Redis
│
└─ 大型项目（10+人）
   ├─ 微服务 → NestJS微服务 + gRPC + Kubernetes
   └─ 前端复杂 → Next.js + 微前端 + 性能监控
```

### 7.2 技术栈推荐

| 项目规模 | 前端技术 | 后端技术 | 数据库 | 部署方案 |
|---------|---------|---------|--------|---------|
| 小型 | React + TS + Tailwind | Express + MongoDB | MongoDB | Vercel/Netlify |
| 中型 | Next.js + TS + Zustand | NestJS + PostgreSQL | PostgreSQL + Redis | Vercel + Render |
| 大型 | Next.js + 微前端 | NestJS微服务 + gRPC | 分布式数据库 | Kubernetes + CI/CD |

### 7.3 最佳实践

| 实践 | 说明 |
|------|------|
| **类型安全** | 使用TypeScript，确保类型安全 |
| **模块化设计** | 按功能划分模块，职责单一 |
| **错误处理** | 统一错误处理，返回一致的错误格式 |
| **日志记录** | 记录请求日志和错误日志 |
| **安全配置** | 使用安全头，防止常见攻击 |
| **性能优化** | 使用缓存，优化数据库查询 |
| **测试覆盖** | 编写单元测试和集成测试 |
| **文档完善** | 使用Swagger生成API文档 |

---

**文档最后更新**：2026年3月  
**版本**：v1.0  
**作者**：Qwen Code
