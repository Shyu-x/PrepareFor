# 第2卷-框架生态

## 第6章 Next.js框架

### 6.10 认证与安全

---

## 6.10 认证与安全

### 6.10.1 认证方案

**参考答案：**

**认证方案对比：**

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  
┃                    认证方案对比                                   ┃
┃                                                                  ┃ 
┃  1. NextAuth.js (Auth.js)                                       ┃  
┃     ┣━━ 多Provider支持                                           ┃ 
┃     ┣━━ 简单易用                                                ┃  
┃     ┗━━ 活跃维护                                                ┃  
┃                                                                  ┃ 
┃  2. Clerk                                                       ┃  
┃     ┣━━ 完整 UI 组件                                            ┃  
┃     ┣━━ 用户管理                                                ┃  
┃     ┗━━ 托管页面                                                ┃  
┃                                                                  ┃ 
┃  3. Auth0                                                       ┃  
┃     ┣━━ 企业级                                                  ┃  
┃     ┣━━ 复杂集成                                                ┃  
┃     ┗━━ 收费模式                                                ┃  
┃                                                                  ┃ 
┃  4. 自行实现 (JWT + Middleware)                                 ┃  
┃     ┣━━ 完全控制                                                ┃  
┃     ┣━━ 需要更多开发                                            ┃  
┃     ┗━━ 适合特殊需求                                            ┃  
┃                                                                  ┃ 
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  

```

---

### 6.10.2 NextAuth.js (Auth.js)

**参考答案：**

**安装：**

```bash
npm install next-auth
```

**基础配置：**

```tsx
// src/app/api/auth/[...nextauth]/route.ts
import NextAuth from 'next-auth'
import GitHubProvider from 'next-auth/providers/github'
import GoogleProvider from 'next-auth/providers/google'
import CredentialsProvider from 'next-auth/providers/credentials'
import { PrismaAdapter } from '@auth/prisma-adapter'
import { prisma } from '@/lib/prisma'

export const { handlers, auth, signIn, signOut } = NextAuth({
  adapter: PrismaAdapter(prisma),
  providers: [
    GitHubProvider({
      clientId: process.env.GITHUB_ID!,
      clientSecret: process.env.GITHUB_SECRET!,
    }),
    GoogleProvider({
      clientId: process.env.GOOGLE_ID!,
      clientSecret: process.env.GOOGLE_SECRET!,
    }),
    CredentialsProvider({
      name: 'credentials',
      credentials: {
        email: { label: 'Email', type: 'email' },
        password: { label: 'Password', type: 'password' },
      },
      async authorize(credentials) {
        // 自定义验证逻辑
        const user = await verifyUser(credentials)
        return user
      },
    }),
  ],
  session: {
    strategy: 'jwt',
  },
  pages: {
    signIn: '/login',
    error: '/error',
  },
})
```

**保护路由：**

```tsx
// src/middleware.ts
import { auth } from '@/auth'

export default auth((req) => {
  const isLoggedIn = !!req.auth
  const isOnDashboard = req.nextUrl.pathname.startsWith('/dashboard')

  if (isOnDashboard && !isLoggedIn) {
    return Response.redirect(new URL('/login', req.url))
  }

  return NextResponse.next()
})

export const config = {
  matcher: ['/dashboard/:path*'],
}
```

**使用 Session：**

```tsx
// 获取当前用户
import { auth } from '@/auth'

export default async function Page() {
  const session = await auth()

  if (!session) {
    return <p>请先登录</p>
  }

  return <p>欢迎, {session.user?.name}</p>
}
```

---

### 6.10.3 中间件认证

**参考答案：**

```tsx
// src/middleware.ts
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'
import { auth } from '@/auth'

export default auth((req) => {
  const isLoggedIn = req.auth
  const isOnLogin = req.nextUrl.pathname.startsWith('/login')

  if (isOnLogin) {
    if (isLoggedIn) {
      return NextResponse.redirect(new URL('/dashboard', req.url))
    }
    return NextResponse.next()
  }

  if (!isLoggedIn) {
    return NextResponse.redirect(new URL('/login', req.url))
  }

  return NextResponse.next()
})

export const config = {
  matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
}
```

---

### 6.10.4 安全最佳实践

**参考答案：**

**CSP (Content Security Policy)：**

```tsx
// next.config.ts
const nextConfig = {
  async headers() {
    return [
      {
        source: '/:path*',
        headers: [
          {
            key: 'Content-Security-Policy',
            value: `
              default-src 'self';
              script-src 'self' 'unsafe-inline' 'unsafe-eval';
              style-src 'self' 'unsafe-inline';
              img-src 'self' blob: data: https:;
              font-src 'self';
              connect-src 'self' https://api.example.com;
            `.replace(/\s{2,}/g, ' ').trim(),
          },
        ],
      },
    ]
  },
}
```

**安全头部：**

```tsx
// next.config.ts
const nextConfig = {
  async headers() {
    return [
      {
        source: '/:path*',
        headers: [
          { key: 'X-Frame-Options', value: 'DENY' },
          { key: 'X-Content-Type-Options', value: 'no-sniff' },
          { key: 'X-XSS-Protection', value: '1; mode=block' },
          { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
          { key: 'Permissions-Policy', value: 'camera=(), microphone=()' },
        ],
      },
    ]
  },
}
```

---

### 6.10.5 环境变量安全

**参考答案：**

**敏感数据处理：**

```bash
# .env.local（不提交）
DATABASE_URL="postgresql://..."
API_SECRET_KEY="xxx"
JWT_SECRET="xxx"
```

**Vercel 环境变量：**

- 在 Vercel Dashboard 中配置
- 自动加密存储
- 支持预览部署独立配置

---

### 6.10.6 面试常见问题

**Q1: 如何实现完整的认证流程？**

1. 使用 NextAuth.js 配置 Provider
2. Middleware 保护路由
3. Session 获取用户信息
4. API 路由验证权限

**Q2: JWT vs Session 怎么选择？**

| 方案 | 优点 | 缺点 |
|:---|:---|:---|
| JWT | 无状态，扩展性好 | 注销困难 |
| Session | 简单，服务器控制 | 需要存储 |

**Q3: 常见安全漏洞如何防范？**

- XSS: CSP + httpOnly cookie
- CSRF: SameSite cookie
- SQL Injection: ORM/参数化查询

---

> **面试提示**：认证和安全是企业级应用的核心，需要理解各种认证方案的优缺点和安全最佳实践。
